# 开发进度

## 当前总览

- 当前阶段：MVP
- 当前重点模块：后端项目骨架与共享约定
- 当前分支：feature/backend-foundation
- 最近更新时间：2026-06-23

## 模块状态

| 模块 | 状态 | 分支 | 负责人/Agent | 备注 |
|---|---|---|---|---|
| 后端项目骨架 | 开发中 | feature/backend-foundation | Codex | 已搭建 FastAPI 基础应用、Python 3.11 元数据、统一响应、trace_id、健康检查 |
| 鉴权模块 | 未开始 |  |  | 下一步优先阅读鉴权接口与库表文档后推进 |
| 地图模块 | 未开始 |  |  |  |
| 任务模块 | 未开始 |  |  |  |
| 猫咪库模块 | 未开始 |  |  |  |
| 个人中心模块 | 未开始 |  |  |  |
| 管理员模块 | 未开始 |  |  |  |
| 通知模块 | 未开始 |  |  |  |
| 排班与任务指派 | 未开始 |  |  |  |

## 最近进展

### 2026-06-23

- 分支：feature/backend-foundation
- 完成：阅读 AGENTS.md、项目说明文档、库表设计说明、接口设计规范、backend README；创建后端基础开发分支；确认 backend 目录目前只有 README。
- 修改文件：docs/开发进度.md
- API 或表变更：无
- 迁移：无
- 验证：已执行 `git status --porcelain` 确认创建分支前工作区干净；已执行 `git branch --list` 确认目标分支未占用。
- 阻塞：无
- 下一步：搭建后端基础骨架；若先做鉴权，需要先阅读 `docs/接口文档/鉴权模块_接口文档.md` 与 `docs/库表文档/鉴权模块_库表设计文档.md`。

- 分支：feature/backend-foundation
- 完成：搭建 FastAPI 后端骨架；指定 Python 版本为 3.11；新增 `pyproject.toml`、应用入口、V1 路由、统一成功响应、`X-Trace-Id` 中间件、健康检查接口和测试；补充后端 README 启动与测试命令。
- 修改文件：`backend/.python-version`、`backend/pyproject.toml`、`backend/README.md`、`backend/app/**`、`backend/tests/test_health.py`、`docs/plans/2026-06-23-backend-foundation.md`、`docs/开发进度.md`
- API 或表变更：新增 `GET /api/v1/health`；未新增业务表。
- 迁移：无。
- 验证：`py -3.11 -m pip install -e ".[dev]"` 通过；`py -3.11 -m pytest -q` 通过，2 个测试通过；`py -3.11 -c "from app.main import app; print(app.title)"` 输出 `Campus Cat Association Map API`。
- 阻塞：无；数据库连接配置已在后续步骤补充，本分支仍未处理 SQLAlchemy、Alembic 和鉴权。
- 下一步：接入 SQLAlchemy/Alembic；或先阅读鉴权接口与库表文档，推进验证码、登录、JWT 基础能力。

## 模块启动检查

- 正在变更的模块：后端项目骨架与共享约定。
- 已读文档：`AGENTS.md`、`docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`backend/README.md`。
- 需要或影响的 API：暂不新增业务 API；后端骨架需预留 `/api/v1` 路由前缀、统一响应 envelope 与 trace_id。
- 需要或影响的表：暂不新增业务表；后续数据库基础需支持 PostgreSQL、PostGIS、UUID、Alembic 迁移。
- 相关页面原型或素材：无。
- 依赖的上游模块：无。
- 最小可用垂直切片：FastAPI 应用入口、配置、统一响应、健康检查、数据库连接和 Alembic 初始化。
- 本分支明确不做：公网注册、第三方登录、Redis、WebSocket、复杂 Web 管理后台、业务模块完整实现。
- 验证方式：后端测试、格式检查、类型/静态检查、FastAPI 启动检查、Alembic 命令检查。

### 2026-06-23

- 分支：feature/backend-foundation
- 完成：服务器侧停止旧 PostgreSQL 容器 `postgresql_zsm5-postgresql_zsm5-1`（未删除），并将其重启策略改为 `no`；新建并启动 `catmap-postgis` 容器，镜像 `postgis/postgis:16-3.4`，宿主机端口继续使用 `35432`；创建 `catmap` 数据库和 `catmap_app` 用户；启用 `postgis` 与 `pgcrypto` 扩展；写入后端开发数据库环境变量文件。
- 修改文件：`backend/app/core/config.py`、`backend/.env`、`backend/.env.example`、`backend/README.md`、`docs/开发进度.md`
- API 或表变更：无业务 API 和业务表变更；新增配置项 `CATMAP_DATABASE_*`。
- 迁移：无。
- 验证：服务器 `docker ps` 确认 `catmap-postgis` 运行且 `35432->5432`，旧容器为 `exited` 且重启策略为 `no`；`psql` 确认当前库为 `catmap`、用户为 `catmap_app`；`pg_extension` 确认 `postgis 3.4.3` 与 `pgcrypto 1.3` 已安装；外部 TCP 探测确认 `203.0.113.10:35432` 可连接。
- 阻塞：后端尚未接入 SQLAlchemy/Alembic，当前只完成数据库环境与配置变量。
- 下一步：在后端数据库基础切片中加入 SQLAlchemy、psycopg、Alembic 配置，并用 `CATMAP_DATABASE_URL` 验证迁移连接。

- 分支：feature/backend-foundation
- 完成：修正未提交后端骨架改动停留在 `main` 工作区的问题，将工作区切回 `feature/backend-foundation`；确认 `backend/.env` 仍由 `.gitignore` 忽略，仅准备提交 `backend/.env.example` 模板。
- 修改文件：`backend/.env.example`、`backend/README.md`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：`py -3.11 -m pytest -q` 通过，2 个测试通过；`git check-ignore -v backend\.env` 确认真实配置文件被忽略；`git status --short --ignored backend\.env backend\.env.example` 确认 `.env.example` 可提交且 `.env` ignored。
- 阻塞：无。
- 下一步：提交后端基础骨架和配置模板；后续接入 SQLAlchemy/Alembic。
