# 开发进度

## 当前总览

- 当前阶段：MVP
- 当前重点模块：地图模块 / 任务模块 / 小程序真机兼容
- 当前重点补充：任务图片 COS URL 持久化、旧 localhost 图片 URL 响应兜底、COS 图片内容直出、COS 配置 BOM 兼容与服务器 env 更新、iOS 地图下拉动画兼容
- 当前分支：main
- 最近更新时间：2026-07-02

## 模块状态

| 模块 | 状态 | 分支 | 负责人/Agent | 备注 |
|---|---|---|---|---|
| 前端工程初始化 | 已完成 | feature/frontend-init | Codex | uni-app Vue3 TypeScript 微信小程序空项目已初始化 |
| 前端素材与原型整理 | 已完成 | feature/frontend-init | Codex | 素材、原型、原型代码已迁移到 frontend |
| 前端基础工程层 | 已完成 | feature/frontend-foundation | Codex | 目录规范、环境配置、请求封装、Pinia 用户状态骨架 |
| 登录页与鉴权前端 | 待联调 | feature/map-drawer-drag | Codex | 登录页、验证码、登录 API、登录状态流转，已合入 main；本分支补充 token 过期后清 session 并跳登录页 |
| 加载页与启动流程 | 待联调 | feature/frontend-loading | Codex | 加载页 UI、猫爪动画、加载文案轮播、登录态检查与资源初始化入口，已合入 main |
| 底部导航组件 | 已完成 | main | Codex | 公共底部导航、四个一级页面占位接入，已合入 main |
| 缺省页组件 | 已完成 | main | Codex | 常用缺省状态配置、复用组件、四个一级页面占位替换，已合入 main |
| 后端项目骨架 | 待联调 | feature/backend-foundation | Codex | FastAPI 基础应用、统一响应、trace_id、SQLAlchemy/Alembic 基础设施，已合入 main |
| 鉴权模块 | 待联调 | feature/auth | Codex | 后端核心已实现并通过 HTTP 契约联调，已合入 main |
| 地图模块 | 待联调 | main | Codex | 新增腾讯 POI 搜索/点击解析、附近 POI 推荐、腾讯步行路线和关联 POI 展示；本轮补齐湖北师范大学区域限制、路线 polyline 可见绘制、POI/marker 平滑居中与详情卡去重布局，并按腾讯 walking 官方 polyline 格式修复路线解码；已移除地图页动态导航移动点，恢复 POI/搜索 POI 的地标 SVG 精确标注；本轮将自定义 marker 收敛为图标/标题两档，并在点位详情补任务图片缩略图和应用内大图预览，待微信开发者工具复测。 |
| 任务模块 | 待联调 | codex/fix-task-module-optimizations | Codex | 暑假喂食任务发布/编辑可保存腾讯 POI 关联，任务详情展示关联公共地点；本轮修复列表完成状态筛选、增加日期筛选、移除今日/刷新入口，并补齐详情底部未开始/投喂/已完成三态。 |
| 猫咪库模块 | 待联调 | main | Codex | 已完成列表页、统计/筛选/列表后端接口、触底懒加载和猫咪库初始表迁移；本轮毕业统计接入猫咪库星球 SVG。 |
| 个人中心模块 | 待联调 | main | Codex | 已完成首次登录强制改密、初始化身份、“我的”页、资料编辑和管理员添加账户切片；本轮“我的”页统计与菜单接入用户页 SVG 素材。 |
| 管理员模块 | 待联调 | main | Codex | 管理员入口支持发布/编辑喂食任务，地图页支持长按点位进入位置编辑；任务状态调整已合入 main。 |
| 通知模块 | 未开始 |  |  |  |
| 排班与任务指派 | 未开始 |  |  |  |
| 文件上传模块 | 待联调 | main | Codex | 后端统一图片上传与 COS 写入已验证；本轮修复图片读取/展示接口在 COS 配置缺失时直接报 65008 的问题，已回退到资产变体保存 URL；上传仍要求 COS 配置，已补齐 UTF-8 BOM `.env` 兼容并通过部署脚本更新服务器 env。 |

## 最近进展

### 2026-07-02

- 分支：main
- 模块：文件上传模块 / 后端配置 / 部署脚本
- 完成：排查图片上传报“腾讯云 COS 配置未完成”的根因。`backend/.env` 第一行带 UTF-8 BOM，导致 `CATMAP_TENCENT_COS_SECRET_ID` 被解析成带 BOM 的 key，Pydantic 配置中 `tencent_cos_secret_id` 为空，而 `secret_key/bucket/region/domain` 均已配置。后端配置加载改为 `utf-8-sig`，兼容 BOM env 文件；部署脚本改为传入 `-EnvFile` 时会覆盖更新服务器 `/opt/catmap/backend/.env`，未传 `-EnvFile` 时继续保留远端现有 `.env`，首部署缺失仍报错。已执行 `scripts/deploy-backend.ps1 -EnvFile backend/.env` 更新服务器代码和 env。
- 修改文件：`backend/app/core/config.py`、`backend/tests/test_database.py`、`scripts/deploy-backend.ps1`、`scripts/test-deployment-contracts.ps1`、`docs/开发进度.md`。
- API 或表变更：无新增 API、无数据库结构变更、无 Alembic 迁移。
- 验证：新增 BOM `.env` 读取红灯测试，修复后 `py -3.11 -m pytest tests/test_database.py::test_settings_reads_bom_prefixed_env_file -q` 通过；新增部署契约红灯测试确认旧脚本不能更新已有 `.env`，修复后 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\test-deployment-contracts.ps1` 通过；部署脚本内置本地验证 `py -3.11 -m pytest -q` 通过（88 passed），`py -3.11 -m ruff check .` 通过；服务器健康检查 `GET http://203.0.113.10/api/v1/health` 返回 `code=0`；服务器运行目录配置检查确认 `COS_SECRET_ID/SECRET_KEY/BUCKET/REGION` 均为 SET，`TencentCosObjectStorage(get_settings())` 初始化成功。
- 阻塞：未直接通过真实小程序上传一张图片做端到端验证；当前已确认不再卡在“COS 配置未完成”这一层。
- 下一步：在小程序端重新上传任务图片，确认接口进入真实 COS 上传流程；若再报 `上传 COS 失败`，继续按 trace_id 查腾讯 COS 权限、Bucket、Region 或密钥权限。

### 2026-07-02

- 分支：main
- 模块：分支收口 / 文件上传模块 / 任务图片展示 / 地图 iOS 兼容
- 完成：按最新 `docs/开发进度.md` 作为冲突处理依据，保留当前 main 上的 COS 图片 URL 持久化、旧 localhost 图片 URL 响应兜底、地图 PNG 图标、`filterMenu` WXS 下拉动画和 API 环境解析方案；合并 `codex/backend-deploy`、`feature/map-dropdown-optimization`、`fix/map-filter-menu`、`feature/map-filter-optimization` 到 main，`feature/optimize-map-dropdown` 已随同名地图下拉优化提交一并覆盖为已合并状态。合并后 `git branch --no-merged main` 与 `git branch -r --no-merged main` 均为空。
- 修改文件：`.gitignore`、`frontend/tests/api/files.test.ts`、`docs/开发进度.md`；合并提交中还纳入部署脚本/文档和历史分支内容，但冲突处以最新进度对应实现为准。
- API 或表变更：无新增 API、无数据库结构变更、无 Alembic 迁移。
- 验证：`py -3.11 -m pytest -q`（backend，87 passed）通过；`py -3.11 -m ruff check .`（backend）通过；`npm run test`（frontend，26 files / 188 tests）通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在 iOS 真机重新导入 `frontend/dist/build/mp-weixin` 做手动复测；`ssl_key/` 已加入 `.gitignore`，目录内容未纳入提交。
- 下一步：在微信开发者工具刷新构建产物并用 iOS 真机复测任务详情图片、地图点 PNG 图标、筛选下拉和底部导航贴底位置；如需要物理清理历史 localhost 图片 URL，可后续单独做一次数据库回填脚本。

### 2026-07-02

- 分支：main
- 模块：文件上传模块 / 暑假喂食任务模块 / 地图模块图片展示
- 已读文档：`AGENTS.md`、`docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`docs/接口文档/文件上传模块_接口文档.md`、`docs/库表文档/图片上传模块_库表优化设计文档.md`、`docs/模块功能/暑假喂食任务模块_功能说明文档.md`、`docs/接口文档/暑假喂食任务模块_接口文档.md`。
- 完成：修复任务详情响应中图片 `file_url` 混入 `http://localhost:8000/api/v1/files/assets/.../content` 的问题。根因是前端上传后把 `asset_id` 派生为 API content 地址并作为业务图片 URL 提交，后端任务照片和打卡照片又原样持久化。现在前端新上传任务图/打卡图直接提交上传接口返回的 COS `default_url/default_thumb_url`；后端在 `file_id` 存在时以 `file_assets.default_url/default_thumb_url` 为准写入 `task_photos`、`task_checkin_photos`，并在任务详情、任务列表、地图点摘要和地图 marker 响应中按 `file_id` 派生 COS URL，兜底已落库的旧 localhost 数据。
- 修改文件：`backend/app/modules/tasks/models.py`、`backend/app/modules/tasks/service.py`、`backend/app/modules/map/service.py`、`backend/tests/test_tasks_api.py`、`frontend/src/pages/tasks/task-page.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`。
- API 或表变更：无新增接口、无表结构变化、无 Alembic 迁移；仅调整已有任务/地图响应中的图片 URL 来源策略。
- 验证：先新增红灯测试确认旧实现会把 content/local URL 写入任务照片/打卡照片并在旧脏数据响应中原样返回；实现后 `py -3.11 -m pytest tests/test_tasks_api.py -q` 通过（14 passed），`npm run test -- --run tests/pages/tasks-page.test.ts` 通过（28 passed）。后续提交前还需跑完整后端/前端验证。
- 阻塞：线上或测试库中已经持久化的旧 `localhost` 记录不会自动改库，但当前响应会在存在 `file_id` 且资产仍存在时返回 COS URL；如需数据库物理清理，可另做一次数据回填脚本。
- 下一步：运行全量验证后提交；确认 `ssl_key/` 保持未跟踪且不进入提交；再合并 main 外未合并分支。

### 2026-07-02

- 分支：main（合并自 `codex/backend-deploy`）
- 模块：后端部署与临时 HTTP 联调
- 完成：合入服务器部署相关产物，包括 Nginx 反代配置、systemd 服务模板、本机部署脚本、部署契约测试脚本和部署方案文档；保留前端 API 域名由 `VITE_API_BASE_URL` 配置的方式，`.env.example` 标注临时 `http://203.0.113.10/api/v1` 联调用法，同时保留小程序正式/体验环境应使用 HTTPS 合法 request 域名的提醒。
- 修改文件：`deploy/nginx/catmap.conf`、`deploy/systemd/catmap-backend.service`、`scripts/bootstrap-server-ssh-key.ps1`、`scripts/deploy-backend.ps1`、`scripts/test-deployment-contracts.ps1`、`docs/plans/2026-07-02-backend-deployment-design.md`、`docs/plans/2026-07-02-backend-deployment.md`、`frontend/.env.example`、`docs/开发进度.md`。
- API 或表变更：无新增 API；无数据库表结构变化。
- 验证：分支合并后将重新运行后端/前端关键验证。
- 阻塞：`ssl_key/` 中的证书和私钥文件保持未跟踪，不能提交；HTTP+IP 只适合临时联调，小程序上线前仍需 HTTPS 域名。
- 下一步：继续合并剩余未进 main 的地图下拉/筛选分支，并在全部合并后跑完整验证。

### 2026-07-02

- 分支：main
- 模块：文件上传模块 / 任务模块图片展示 / 地图模块
- 已读文档：`AGENTS.md`、`docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`docs/接口文档/文件上传模块_接口文档.md`、`docs/库表文档/图片上传模块_库表优化设计文档.md`、`docs/模块功能/地图模块_详细功能说明.md`、`docs/模块功能/暑假喂食任务模块_功能说明文档.md`。
- 完成：继续修复 iOS 真机图片展示和地图下拉动画问题。确认图片仍不可展示的根因不是未使用腾讯 COS，而是 `GET /api/v1/files/assets/{asset_id}/content` 在 COS storage 可用时返回 307 跳转到 COS 签名 URL，小程序 `<image>` 真机容易受跨域跳转、COS 合法域名和签名 URL 限制影响。本轮改为 storage 可用时由后端读取 COS 对象并直接以 `image/jpeg` 内容返回，小程序侧仍只访问 API 域名；storage 不可用时保留旧的保存 URL redirect 作为开发兜底。地图筛选下拉动画移除 WXS 里的 `transition: all`、动态 `pointerEvents` 和 `scaleY` 组合，改为只动画 opacity/translate 与箭头 rotate，菜单点击态用 Vue/CSS class 控制，降低 iOS 原生 map cover 层动画异常概率。
- 修改文件：`backend/app/api/v1/files.py`、`backend/app/modules/files/service.py`、`backend/app/modules/files/storage.py`、`backend/tests/test_files_api.py`、`frontend/src/pages/index/filter-menu.wxs`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`。
- API 或表变更：无数据库表结构变化，无 Alembic 迁移；`GET /api/v1/files/assets/{asset_id}/content` 行为调整为“有 COS storage 时直出图片内容，storage 不可用时继续 redirect 保存 URL”。
- 验证：先新增红灯测试确认旧实现仍 307 跳转 COS、WXS 仍含 iOS 高风险动画写法；实现后目标测试通过。最终 `py -3.11 -m pytest backend/tests/test_files_api.py -q` 通过（12 passed）；`py -3.11 -m pytest backend/tests -q` 通过（84 passed）；`py -3.11 -m ruff check backend` 通过；`npm run test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`npm run type-check` 通过；`npm run test` 通过（26 files，188 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过；扫描确认 WXS 源码和小程序构建产物不再包含 `transition: trans`、`pointerEvents`、`scaleY` 或 `transition || 'none'`。
- 未通过/未验证：未在 iOS 真机直接复测 COS 图片加载和下拉开合动画；需要刷新 `frontend/dist/build/mp-weixin` 后实测。
- 阻塞：无自动化阻塞；真机仍需确认 API 域名在微信小程序 request/download 合法域名中。
- 下一步：重新导入微信开发者工具或刷新构建产物，在 iOS 真机点开任务详情和地图点卡片，确认图片通过 API content 地址显示；连续开合地图筛选下拉，确认不再闪烁、卡死或无法点击。

### 2026-07-02

- 分支：main
- 模块：地图模块 / 任务模块 / 底部导航组件 / 文件图片展示
- 已读文档：`AGENTS.md`、`docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`docs/模块功能/地图模块_详细功能说明.md`、`docs/模块功能/暑假喂食任务模块_功能说明文档.md`、`docs/接口文档/文件上传模块_接口文档.md`、`docs/接口文档/暑假喂食任务模块_接口文档.md`、`docs/库表文档/图片上传模块_库表优化设计文档.md`、`frontend/页面原型/地图模块/下拉菜单.png`、`frontend/页面原型/任务模块/任务详情.png`。
- 完成：修复 iOS 真机无法显示地图点相关 SVG 的问题，将地图点筛选、地图 marker、管理员选点 marker、任务/猫咪筛选箭头统一切换为由原 SVG 渲染出的 PNG 资源；任务详情图片新增 `file_id -> /files/assets/{asset_id}/content` 展示 URL helper，并过滤 `/files/images` 上传接口作为图片地址的旧坏数据，避免图片 GET 到上传接口触发 500；底部导航去掉自身 `safe-area-inset-bottom` padding，改为更贴底的固定 `8rpx` 间距。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/admin/tasks/location.vue`、`frontend/src/pages/cats/index.vue`、`frontend/src/pages/tasks/index.vue`、`frontend/src/pages/tasks/detail.vue`、`frontend/src/pages/tasks/task-page.ts`、`frontend/src/components/AppTabBar.vue`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/tasks-page.test.ts`、`frontend/tests/pages/cats-page.test.ts`、`frontend/tests/components/app-tab-bar.test.ts`、`frontend/素材/png/地图点/*`、`frontend/素材/png/菜单/定位.png`、`docs/开发进度.md`。
- API 或表变更：无后端 API、数据库表或 Alembic 迁移变更；前端继续使用既有 `GET /api/v1/files/assets/{asset_id}/content` 图片内容入口。
- 验证：先补红灯测试确认旧实现仍引用地图点 SVG、任务详情会接受 `/files/images` 坏图地址、底部导航仍使用 safe area；实现后 `npm run test -- --run tests/pages/map-page.test.ts tests/pages/tasks-page.test.ts tests/pages/cats-page.test.ts tests/components/app-tab-bar.test.ts` 通过（97 passed）；`npm run test -- --run tests/api/files.test.ts tests/pages/tasks-page.test.ts` 通过（31 passed）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run test` 前端全量通过（26 files，188 tests）；扫描确认 `frontend/src` 不再引用 `素材/svg/地图点`，构建产物中地图点 SVG 未残留。
- 未通过/未验证：未在 iOS 真机或微信开发者工具里重新导入构建产物手动复测下拉菜单、地图 marker、任务详情图片和底部导航实际位置。
- 阻塞：无自动化阻塞。
- 下一步：导入或刷新 `frontend/dist/build/mp-weixin`，在 iOS 真机复测地图筛选下拉、地图点 marker、管理员地图选点、任务详情顶部图和完成照片预览，并确认底部导航贴底距离符合预期。

### 2026-07-02

- 分支：main
- 模块：前端基础工程层 / API 域名配置
- 完成：排查微信开发者工具中 `GET http://localhost:8000/api/v1/auth/captcha net::ERR_CONNECTION_REFUSED`，确认根因不是验证码接口单点路径，而是前端 `appEnv.apiBaseUrl` 在小程序构建缺少 `VITE_API_BASE_URL` 时仍回落 `http://localhost:8000/api/v1`；新增 `frontend/src/config/api-env.ts` 统一解析 API base URL，开发模式允许本地 fallback，生产构建缺少 HTTPS 域名或显式配置 localhost 时直接报错；`vite.config.ts` 在构建阶段执行同一校验，避免继续产出 localhost 小程序包；更新 `.env.example` 和前端 README，说明正式小程序构建必须配置 `frontend/.env.production` 或环境变量 `VITE_API_BASE_URL=https://<后端域名>/api/v1`。
- 修改文件：`frontend/src/config/api-env.ts`、`frontend/src/config/app-env.ts`、`frontend/vite.config.ts`、`frontend/tests/config/app-env.test.ts`、`frontend/.env.example`、`frontend/README.md`、`docs/开发进度.md`
- API 或表变更：无后端 API 或数据库表结构变更；仅前端构建配置与环境校验调整。
- 验证：先新增红灯测试确认缺少 `resolveApiBaseUrl`；实现后 `npm test -- --run tests/config/app-env.test.ts` 通过（6 tests）；无域名执行 `npm run build:mp-weixin` 现在按预期失败并提示配置 `VITE_API_BASE_URL`；`npm test -- --run` 通过（26 files，183 tests）；`npm run type-check` 通过；`git diff --check` 通过；扫描确认 `frontend/dist/build/mp-weixin` 无 `http://localhost:8000`，`frontend/src/api` 除 `routes.ts` 外无后端路径硬编码，所有普通请求和上传请求都经 `appEnv.apiBaseUrl`。
- 未通过/未验证：仓库未提供真实 HTTPS API 域名，因此未使用真实域名生成可直接导入微信开发者工具的最终小程序包。
- 阻塞：需要在本机或部署环境配置真实 `VITE_API_BASE_URL` 后重新构建。
- 下一步：创建被 `.gitignore` 忽略的 `frontend/.env.production`，写入真实后端域名后执行 `npm run build:mp-weixin`，并在微信开发者工具中刷新 `frontend/dist/build/mp-weixin`。

### 2026-07-02

- 分支：main
- 模块：前端基础工程层 / API 请求封装
- 完成：新增 `frontend/src/api/routes.ts` 作为前端后端接口路径统一登记表，集中管理鉴权、地图、任务、猫咪库、个人中心、管理员和文件接口路径；将 `frontend/src/api/*.ts` 中散落的请求路径改为引用 `API_ENDPOINTS`；统一抽出查询参数 compact helper，并保留管理员地图点更新 payload 中 `null` 清空字段的语义；补充 `frontend/tests/api/routes.test.ts` 覆盖静态路径、动态 ID 编码、文件资源查询串和参数过滤规则。
- 修改文件：`frontend/src/api/routes.ts`、`frontend/src/api/auth.ts`、`frontend/src/api/admin-users.ts`、`frontend/src/api/admin-map.ts`、`frontend/src/api/cats.ts`、`frontend/src/api/files.ts`、`frontend/src/api/map.ts`、`frontend/src/api/me.ts`、`frontend/src/api/profile.ts`、`frontend/src/api/tasks.ts`、`frontend/tests/api/routes.test.ts`、`docs/开发进度.md`
- API 或表变更：无后端 API 或数据库表结构变更；仅前端请求路径组织方式调整。
- 验证：先新增红灯测试确认 `@/api/routes` 缺失；实现后 `npm test -- --run tests/api/routes.test.ts` 通过；`npm test -- --run tests/api` 通过（9 files，35 tests）；`npm run type-check` 通过；`npm test -- --run` 通过（26 files，179 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过；`rg` 确认 `frontend/src/api` 除 `routes.ts` 外无 `url: "/..."` 后端路径硬编码。
- 阻塞：无。
- 下一步：后续新增前端 API 时优先在 `frontend/src/api/routes.ts` 登记路径，再由模块 API 文件引用，避免页面或业务模块继续手写后端 endpoint。

### 2026-07-02

- 分支：codex/fix-task-module-optimizations
- 模块：任务模块
- 已读文档：`AGENTS.md`、`docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`docs/模块功能/暑假喂食任务模块_功能说明文档.md`、`docs/接口文档/暑假喂食任务模块_接口文档.md`、`docs/库表文档/暑假喂食任务模块_库表设计文档.md`、`frontend/页面原型/任务模块/任务详情.png`、`frontend/页面原型/任务模块后期/任务.png`。
- 完成：修复任务列表完成状态筛选反向/混乱问题，将“完成状态”下拉改为筛选 `task_execution_dates.status`，选“进行中”只查待投喂执行日，选“已完成”只查已完成执行日；任务列表新增第三个日期下拉，支持全部、本日、本周、本月和特定日期；移除任务列表页“今日任务”“刷新”按钮及对应状态/函数；将“全部任务类型”“全部完成状态”文案统一为“全部”；详情完成投喂增加日期限制，未来执行日不可提前完成；任务动态内容中的“于某日”改为真实提交日期；详情底部主按钮改为未开始（红色、不可点）、投喂（绿色、可点）、已完成（灰色、不可点）。
- 修改文件：`backend/app/api/v1/tasks.py`、`backend/app/api/v1/admin_tasks.py`、`backend/app/modules/tasks/service.py`、`backend/tests/test_tasks_api.py`、`frontend/src/api/tasks.ts`、`frontend/src/pages/tasks/index.vue`、`frontend/src/pages/tasks/detail.vue`、`frontend/src/pages/tasks/task-page.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`。
- API 或表变更：`GET /api/v1/tasks` 与 `GET /api/v1/admin/tasks` 新增查询参数 `execution_status`、`execute_date_start`、`execute_date_end`；无数据库表结构变化，无 Alembic 迁移。
- 验证：先新增红灯测试确认旧实现存在问题；实现后 `py -3.11 -m pytest tests/test_tasks_api.py -q` 通过（11 passed）；`py -3.11 -m pytest -q` 通过（84 passed）；`py -3.11 -m ruff check .` 通过；`npm test -- --run tests/pages/tasks-page.test.ts` 通过（24 passed）；补齐当前地图页定位缩放、原生 marker callout、聚焦缩放契约后 `npm test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`npm test -- --run` 前端全量通过（25 files，174 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 未通过/未验证：未在微信开发者工具或真机手动复测任务列表筛选、日期筛选、未来日期未开始按钮和当天投喂完成流；未手动复测地图页定位/marker callout 视觉表现。
- 阻塞：无自动化阻塞；当前工作区按用户要求准备提交全部已有改动并合入 main。
- 下一步：合入 `main` 后重新运行关键验证；随后在微信开发者工具刷新 `frontend/dist/build/mp-weixin`，手动复测任务列表与详情投喂流程。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：继续修正自定义 marker 缩放气泡。上一轮改动后出现两个新问题：标题阈值恢复到 19 导致实际小程序缩放达不到标题条件，气泡始终不出现；筛选切换时主动 `MapContext.getScale()` 并写回 `mapScale`，导致点击“无标记”也会触发地图缩放调整。本轮将标题阈值回到 18，但移除所有非手动缩放路径对 `mapScale` 的写入：`focusMapToPoint` 只负责平滑居中，不再设置聚焦缩放；“我的位置”只负责回中，不再强制缩放；`selectFilter` 只清选中态并切换筛选，不再读取或写回地图 scale。当前 `mapScale` 只在初始化和原生缩放事件同步时更新，避免点击 marker 或切换筛选污染气泡显示判断。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：按用户要求，本轮未新增测试、未运行测试、未运行构建；仅做源码静态检查确认点击 marker、下拉筛选、“我的位置”不再写 `mapScale`。
- 阻塞：未做微信开发者工具/真机视觉复测。
- 下一步：刷新小程序后复测：下拉“无标记”不应自动改变地图缩放；首次进入后手动放大到近距离应出现气泡，缩小应消失；点击 marker 只显示选中 marker 的气泡，不应影响后续筛选状态。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：根据复现路径“初次进入手动缩放不出气泡；点击 marker 后气泡出现；切换到无标记再切回 marker 类型后气泡常驻且不随缩放消失”继续排查。确认根因不是单纯 callout 样式，而是两条状态链路叠加：一是 `markertap` 会通过 `focusMapToPoint` 把 `mapScale` 程序化写到 18，旧阈值 18 等于让筛选重建后的未选中 marker 也进入标题模式；二是 `handleMapRegionChange` 只读取 `event.detail.scale/causedBy`，但不同端可能把缩放字段放在事件根对象或 `detail.detail`，导致手势缩放无法回写 `mapScale`；三是原生 marker id 使用数组下标，筛选清空再恢复时复用旧 id，容易继承原生 map 的 callout 状态。本轮恢复标题阈值为 19；新增 `getMapRegionChangeDetail` 兼容 `event.detail`、`event.detail.detail`、事件根对象三种结构；筛选切换时先清选中态并通过 `MapContext.getScale()` 同步当前真实缩放再应用筛选；marker id 改为基于点位 key 的稳定数字 id，点击/长按按稳定 id 反查 marker，不再用 `index + 1`。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：按用户要求，本轮未新增测试、未运行测试、未运行构建；仅做源码静态检查确认关键路径已落地。
- 阻塞：未做微信开发者工具/真机视觉复测。
- 下一步：刷新小程序后按复现路径验证：首次进入手动缩放到近距离应出现标题；缩小应消失；点击 marker 后只该 marker 标题常驻；切到无标记再切回任务/完成任务时，未选中 marker 应按当前真实缩放决定是否显示标题，不应继承旧气泡。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：根据复测反馈“地图缩小到校园级范围仍有气泡”继续修复 marker 标题隐藏逻辑。根因是微信小程序原生 `map` 的 marker callout 会缓存已显示过的气泡；旧实现隐藏标题时直接移除 marker 的 `callout` 字段，原生层不一定同步清除旧气泡，导致看起来像缩放条件失效。本轮改为所有 marker 始终显式下发 callout 状态：需要标题时使用 `display: "ALWAYS"` 和标题内容；不需要标题时使用 `display: "BYCLICK"` 和空内容，强制原生 map 把旧气泡收回。
- 修改文件：`frontend/src/pages/index/index.vue`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：按用户要求，本轮未新增/修改测试，未运行构建或自动验证，由用户在微信开发者工具/真机视觉复测。
- 阻塞：未做自动验证。
- 下一步：视觉复测缩放：缩小到校园级范围时不应残留气泡；继续放大到标题阈值后气泡出现；再次缩小后气泡应被显式收回。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：根据复测反馈“地图放大缩小，气泡都没有出现”继续修复自定义 marker 标题气泡。根因确认是上一轮把 `MARKER_LABEL_MIN_VISIBLE_ZOOM` 抬到 19，而地图点位聚焦和用户常见手势缩放实际停在 18 左右，导致未点击 marker 时很难进入标题模式；同时微信/uni-app 原生 `regionchange` 在部分缩放结束事件里可能不携带 `scale`，前端无法及时知道用户已经放大。本轮将未选中态标题阈值收敛到 18，并新增 `shouldQueryMapScaleFromRegionChange` + `MapContext.getScale()` 兜底同步真实缩放；同时补强选中态规则：存在选中 marker 时，只让选中 marker 的标题常驻，其他 marker 保持图标，避免点击后所有气泡一起出现。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增红灯测试，确认旧实现下 `zoom = 18` 仍不显示标题、旧数据 `label_min_zoom = 19` 会卡死标题、`regionchange` 缺少 `scale` 时不会查询原生地图缩放、选中 marker 后其他 marker 仍可能跟随阈值出现气泡；实现后 `npm test -- --run tests/pages/map-page.test.ts -t "marker display|native marker callouts|syncs native map scale"` 通过（3 passed）；`npm test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm test -- --run` 通过（25 files，172 tests）；`git diff --check` 通过。
- 阻塞：仍未在微信开发者工具/真机直接复测同一地图视野；构建产物已更新到 `frontend/dist/build/mp-weixin`。
- 下一步：刷新小程序构建后复测：未点击 marker 时，缩放到 18 级附近应出现标题，缩小后标题消失；点击某个 marker 后，只钉住这个 marker 的标题，其他 marker 不跟着冒泡。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：修复自定义 marker 标题气泡点击后状态混乱的问题。根因确认有两处：一是缩放标题使用 `customCallout` 插槽批量渲染，微信原生 map 在 marker 点击后容易把已有自定义气泡批量唤起/缓存，导致所有 marker 气泡出现且缩放后不稳定消失；二是任务详情回到地图聚焦点位时 `focusPendingMapPoint` 直接把 `mapPointMarkers` 替换为单个 marker，导致其他 marker 被清空。本轮改为每个 marker 自身使用原生 `callout` 承载标题，未选中 marker 仍只按缩放阈值显示/隐藏，选中 marker 的标题保持常驻；聚焦详情点位改为 `upsertFocusedMapMarker`，不再清空已有 marker 集合。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增红灯测试，确认旧实现下选中 marker 仍被缩放隐藏、页面仍含 `customCallout` 插槽路径、聚焦点位会替换 marker 集合；实现后 `npm test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm test -- --run` 通过（25 files，172 tests）；`git diff --check` 通过；`rg`/`Select-String` 确认当前地图页源码不再包含旧 `customCallout`/`nativeMarkerCallouts`/`slot="callout"` 实现，测试中保留禁止断言，历史进度中保留旧记录。
- 阻塞：未在微信开发者工具/真机直接复测截图中的同一交互路径。
- 下一步：刷新 `frontend/dist/build/mp-weixin` 后复测：未点击 marker 时，缩放到小范围才出现标题、缩小后标题消失；点击某个 marker 后只保留该 marker 的标题常驻，其他 marker 仍显示且未达阈值不出现标题。

### 2026-07-02

- 分支：main
- 模块：地图模块
- 完成：继续修复自定义 marker 标题气泡在大范围地图下仍显示的问题。根因分两层：一是后端/旧数据给出的 `label_min_zoom = 16` 对当前微信原生地图视野来说过早，校园级范围也会显示标题；二是原生 `regionchange` 在某些缩放结束事件中可能没有稳定携带 `causedBy = scale`，旧逻辑只认这个值，导致放大显示范围后 `mapScale` 可能没有及时回落，标题气泡残留。本轮前端新增 `MARKER_LABEL_MIN_VISIBLE_ZOOM = 19`，即使后端返回 16/17/18 也要到更小范围才显示标题；新增 `shouldSyncMapScaleFromRegionChange`，允许真实缩放结束事件同步 scale，同时继续排除拖拽和程序化 update。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增红灯测试，确认旧实现下 `zoom = 17.8/18` 会错误显示标题、缺少 `causedBy = scale` 的缩放结束事件不会同步 scale；实现后 `npm test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`npm run type-check` 通过；`npm test -- --run` 通过（25 files，172 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具/真机直接复测截图中的同一视野；构建产物已生成。
- 下一步：刷新 `frontend/dist/build/mp-weixin` 后复测：截图这种校园/周边大范围视野应只显示 marker 图标；继续放大到更小范围后标题才出现；缩小回大范围后标题消失。

### 2026-07-01

- 分支：main
- 模块：图片预览组件
- 完成：根据微信开发者工具截图反馈，将应用内大图预览的顶部工具栏整体下移，计数器和关闭按钮从 `safe-area + 28rpx` 调整为 `safe-area + 170rpx`，避开状态栏、刘海/灵动岛和右上角胶囊区域。
- 修改文件：`frontend/src/components/ImagePreviewModal.vue`、`frontend/tests/components/image-preview-modal.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增样式红灯测试确认旧偏移量不满足要求；实现后 `npm test -- --run tests/components/image-preview-modal.test.ts` 通过（3 passed）；`npm run build:mp-weixin` 通过；`git diff --check` 通过；构建产物 `dist/build/mp-weixin/components/ImagePreviewModal.wxss` 已确认包含 `top: calc(env(safe-area-inset-top) + 170rpx)`。
- 阻塞：未在微信开发者工具真机/模拟器里直接目测最终距离。
- 下一步：刷新 `frontend/dist/build/mp-weixin` 后查看大图预览页，确认计数器和关闭按钮不再顶到顶部。

### 2026-07-01

- 分支：main
- 模块：微信小程序配置、地图模块
- 完成：修复微信开发者工具控制台报错。根因是新增 `ImagePreviewModal` 组件后，微信开发者工具的代码依赖分析会把 `components/ImagePreviewModal.js` 误判为无依赖文件并过滤，页面运行时引用该组件就报“已被代码依赖分析忽略”；本轮在 `mp-weixin.setting` 中显式关闭 `ignoreDevUnusedFiles` 和 `ignoreUploadUnusedFiles`，并确认生成的 `dist/build/mp-weixin/project.config.json` 已带上配置。同步修复截图中的定位私有 API 报错，在 `mp-weixin.requiredPrivateInfos` 声明 `getLocation`、`startLocationUpdate`、`onLocationChange`，生成的 `app.json` 已包含这些字段。
- 修改文件：`frontend/src/manifest.json`、`frontend/tests/config/miniapp-config.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增配置红灯测试，确认旧 manifest 缺少无依赖过滤开关和定位私有 API 声明；实现后 `npm test -- --run tests/config/miniapp-config.test.ts` 通过（3 passed）；`npm run build:mp-weixin` 通过；`rg` 确认 `dist/build/mp-weixin/project.config.json` 含 `ignoreDevUnusedFiles: false`、`ignoreUploadUnusedFiles: false`，`dist/build/mp-weixin/app.json` 含 `requiredPrivateInfos`；最终 `npm test -- --run` 通过（25 files，171 tests）；`npm run type-check` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具中重新导入/编译后的产物直接观察控制台；代码和构建产物已对齐报错要求。
- 下一步：重新运行 `npm run build:mp-weixin` 后导入或刷新 `frontend/dist/build/mp-weixin`，确认控制台不再出现 `components/ImagePreviewModal.js 已被代码依赖分析忽略` 和 `wx.onLocationChange need to be declared...`。

### 2026-07-01

- 分支：main
- 模块：地图模块、任务详情页
- 完成：新增共用 `ImagePreviewModal` 应用内图片查看器，使用 `movable-area`/`movable-view` 承载大图并开启 1-4 倍缩放，提供关闭按钮、图片计数和左右切换。地图点位详情卡的头像/缩略图不再调用系统 `uni.previewImage`，改为在当前 app 页面内打开大图；任务详情页顶部任务图片轮播和完成照片也接入同一个查看器，可点击查看并缩放。
- 修改文件：`frontend/src/components/ImagePreviewModal.vue`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/tasks/detail.vue`、`frontend/tests/components/image-preview-modal.test.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增红灯测试，确认旧实现下组件不存在、地图点位卡仍依赖 `uni.previewImage`、任务详情图片不可打开应用内预览；实现后目标测试通过。最终 `npm test -- --run tests/components/image-preview-modal.test.ts tests/pages/map-page.test.ts tests/pages/tasks-page.test.ts` 通过（77 passed）；`npm run type-check` 通过；`npm test -- --run` 通过（25 files，169 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具/真机手动验证 `movable-view` 双指缩放、左右切换和关闭按钮触感；小程序构建已通过。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后复测地图点位详情图片、任务详情顶部图和完成照片：点击后应在 app 内出现黑色大图查看器，关闭按钮可退出，双指可放大缩小，多图可左右切换。

### 2026-07-01

- 分支：main
- 模块：地图模块、任务点位详情
- 完成：按反馈优化自定义 marker 展示逻辑：移除第三档 marker 缩略图气泡，只保留“远距离图标点、达到标题阈值后显示标题气泡”两档；标题出现/消失只依据 `label_min_zoom`，不再受选中态、marker 密度、`preview_min_zoom` 或拖拽事件影响；`regionchange` 仅在原生缩放手势 `causedBy = scale` 时同步 `mapScale`，避免拖动地图后反向改变绑定缩放。点位详情卡改为使用任务上传图片：第一张缩略图作为头像，标题右侧展示任务类型标签，其余图片最多展示三张缩略图，1/2/3 张分别自适应铺满、二分、三分，并支持点击 `uni.previewImage` 查看；后端点位摘要优先返回任务图片，任务无图时回退地图点位图片。
- 修改文件：`backend/app/modules/map/service.py`、`backend/tests/test_tasks_api.py`、`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移；复用地图点位摘要已有 `cover_photo_url`、`photos` 字段，并把任务上传图片作为任务点位摘要的优先图片来源。
- 验证：先新增前端 marker 两档、拖拽不改缩放、详情缩略图预览断言，以及后端点位摘要任务图片断言，旧实现下目标用例失败；实现后 `npm test -- --run tests/pages/map-page.test.ts` 通过（53 passed）；`pytest backend/tests/test_tasks_api.py::test_map_point_summary_uses_uploaded_task_photos_for_detail_thumbnails` 通过；`npm run type-check` 通过；`pytest backend/tests/test_map_api.py backend/tests/test_tasks_api.py` 通过（22 passed）。
- 阻塞：未在微信开发者工具/真机手动复测 marker 缩放阈值、拖拽后气泡稳定性和缩略图点击预览观感。
- 下一步：导入微信开发者工具后复测：缩放到阈值标题出现、继续拖动不改变缩放；缩小过阈值标题消失；点击任务 marker 后点位详情头像和三联图展示正确，点击缩略图可预览。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：继续排查点击腾讯底图 POI 后地图反复移动闪烁、网络面板持续出现腾讯地图瓦片请求的问题。复盘最近地标 SVG、POI 去重和平滑回中改动后确认：前几轮主要拦截了重复 `poitap`/`/map/poi/resolve`，但地图仍会抖动的根因在 `handleMapRegionChange`。当前实现为了支持拖拽后“我的位置”从真实视口回中，在每次原生 `regionchange` 结束时无条件把 `centerLocation` 回写到绑定的 `mapCenter`，同时无条件回写 `scale`；而 POI 聚焦/定位本身也会通过响应式 `mapCenter`、`mapScale` 驱动原生 `<map>`，导致“代码设置视口 -> 原生 map 派发 update -> 前端再次写回 props -> 原生 map 再移动/重绘瓦片”的闭环。修复后：程序化 `causedBy = update` 的原生回调不再反向写回中心和缩放；手动拖拽/缩放仍通过带阈值的 `syncMapCenterFromNative`、`syncMapScaleFromNative` 同步；越界拖拽仍会平滑拉回校园限制范围。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增红灯测试 `does not echo programmatic native map updates back into the bound center`，确认旧实现缺少 `syncMapCenterFromNative` 且在 `regionchange` 中无条件 `mapCenter.value = nextCenter`；实现后该测试通过。随后新增 `does not echo programmatic native map scale updates back into the bound scale`，确认旧实现无条件 `mapScale.value = detail.scale`；实现后通过。最终 `npm run test -- --run tests/pages/map-page.test.ts` 通过（52 passed）；`npm run test -- --run` 通过（24 files，165 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：仍未在微信开发者工具/真机网络面板直接观察瓦片请求是否停止刷屏；代码侧已切断原生 map 程序化 `update` 与 Vue 绑定中心/缩放之间的回写闭环。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后重点复测：点击底图 POI 时地图只完成一次聚焦动画并稳定停住；网络面板不再持续刷 `rt*.map.gtimg.com/tile`；地标 SVG 仍落在 POI 坐标；手动拖动地图后点击“我的位置”仍能从当前视口回中。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：继续修复点击腾讯底图 POI 后地图来回滑动、随后点击“我的位置”仍会触发连续报文的问题。二次排查确认上一轮只按同一个 `poiTapKey` 去重仍不够：微信原生 map 在 POI 解析请求未返回前可能连续派发坐标/name 略有差异的 `poitap`，这些事件会穿透精确 key 去重并形成多条 `/map/poi/resolve` 请求；同时用户点击“我的位置”时，之前飞行中的 POI 解析请求可能稍后回写 `selectedSummary`、`selectedPoiMarker` 并再次把地图拉回 POI。修复后：任意 POI 解析进行中时拒绝新的 POI tap；POI 解析响应带请求序号，过期响应不会回写视图；成功选中 POI 或点击“我的位置”时短时间压制原生 POI tap；“我的位置”会先清理选中的外部 POI、待解析 key 和过期请求序号，再平滑回到用户位置。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增/收紧回归测试，确认旧实现缺少 `clearNativePoiSelection`、请求序号、动画期 POI tap 压制和任意 pending 解析门闩；实现后 `npm run test -- --run tests/pages/map-page.test.ts -t "deduplicates native tencent poi taps"` 通过；`npm run test -- --run tests/pages/map-page.test.ts -t "locating the user"` 通过；`npm run test -- --run tests/pages/map-page.test.ts` 通过（50 passed）；`npm run type-check` 通过；`npm run test -- --run` 通过（24 files，163 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：仍未在微信开发者工具/真机网络面板直接观察；代码侧已从“精确 key 去重”升级为“解析中全局门闩 + 过期响应作废 + 动画期压制 + 定位前清理 POI 状态”。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后复测：点击底图 POI 时最多只有一次 `/map/poi/resolve`；地图平滑到 POI 后不再来回滑动；在 POI 动画或解析中点击“我的位置”，不会再被旧 POI 响应拉回，也不会继续发送 POI 解析报文。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：修复点击腾讯底图 POI 后地图不停抖动并连续发送请求的问题。根因是 `handleNativePoiTap` 对原生 `poitap` 没有同点位去重和请求中保护：点击 POI 后会请求 `/map/poi/resolve`，成功后叠加临时地标 marker 并平滑移动视口；在微信原生 map 中该过程可能再次派发同一 POI 的 `poitap`，从而形成“解析请求 -> 设置选中 POI -> 平滑回中 -> 再次 poitap -> 再次解析请求”的循环，表现为地图抖动和调试台报文洪水。本轮为 POI 点击新增归一化 key、`pendingPoiResolveKey` 和 `lastResolvedPoiTapKey`：同一 POI 正在解析或已作为当前选中 POI 时直接忽略重复事件；`pending` 在获取 token 前设置，避免 token 刷新期间重复事件穿透，最终在 `finally` 中清理。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增回归测试 `deduplicates native tencent poi taps so repeated native events do not flood resolve requests`，旧实现下失败并确认缺少 `pendingPoiResolveKey`/`lastResolvedPoiTapKey`/`shouldSkipPoiResolve`；实现后 `npm run test -- --run tests/pages/map-page.test.ts -t "deduplicates native tencent poi taps"` 通过；`npm run test -- --run tests/pages/map-page.test.ts` 通过（49 passed）；`npm run type-check` 通过；`npm run test -- --run` 通过（24 files，162 tests）；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具/真机观察调试台网络面板；代码侧已阻断同一 POI 的重复解析请求。
- 下一步：导入 `frontend/dist/build/mp-weixin`，点击同一个底图 POI 和不同 POI，确认同一点不会连续请求 `/map/poi/resolve`，不同 POI 仍可正常解析、显示地标 marker 并平滑居中。

### 2026-07-01

- 分支：main
- 模块：地图模块、任务模块
- 完成：优化地图页任务抽屉和任务页任务列表展示。地图抽屉中的自定义任务点优先使用任务上传的第一张/封面图片缩略图，标题保持不变，副文案改为地图选点时填写的 `location_detail` 位置补充说明，右侧状态改为当前任务状态：进行中为红色，已完成为绿色。后端地图 marker、搜索结果、底部内容统一下发任务封面、位置补充说明和 `task_status_label`，避免不同入口展示不一致。任务页新增搜索框与两项筛选器，样式对齐猫咪库搜索/筛选组件；第一项筛选任务类型，第二项筛选完成状态，并同步调整任务页状态 pill 配色。
- 修改文件：`backend/app/modules/map/service.py`、`backend/tests/test_map_api.py`、`backend/tests/test_tasks_api.py`、`frontend/src/api/map.ts`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/tasks/index.vue`、`frontend/src/pages/tasks/task-page.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、表或迁移；地图相关响应在既有 `cover_photo_url` 基础上补充任务图优先级，并通过现有 `extra` 扩展 `location_detail`、`location_name`、`task_status_label`。
- 验证：先新增回归断言并观察到旧实现下后端 3 个目标用例失败、前端任务/地图页目标断言失败；实现后 `python -m pytest tests/test_tasks_api.py::test_admin_can_publish_summer_feeding_task_and_map_marker_is_visible tests/test_tasks_api.py::test_map_init_and_points_use_dynamic_feeding_completion_filters tests/test_map_api.py::test_map_bottom_content_returns_latest_task_items -q` 通过；`python -m pytest tests/test_tasks_api.py tests/test_map_api.py -q` 通过（21 passed）；`python -m pytest -q` 通过（79 passed）；`python -m ruff check .` 通过；`npm run test -- --run tests/pages/tasks-page.test.ts tests/pages/map-page.test.ts` 通过（69 passed）；`npm run test -- --run` 通过（24 files，161 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具/真机手动验证任务页 picker 展开态、地图抽屉缩略图实际渲染和任务状态颜色；当前已通过单元测试、类型检查和小程序构建验证。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后手动复测：地图页点击任务点/搜索任务点后抽屉缩略图、位置补充说明、进行中/已完成颜色；任务页搜索标题/喂食点/位置，分别筛选任务类型和完成状态，确认结果刷新和空状态正常。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：优化地图 marker 表现层。点击腾讯底图 POI 或搜索到外部 POI 时，会在对应 POI 坐标追加 `地标.svg` 临时 marker，并通过原生 marker 底部中心锚点对齐到 POI 图标位置；外部 POI marker 仍不参与业务点位编辑，也不会显示标题/图片气泡。自定义业务 marker 接入缩放分级：低缩放仅显示点位 SVG，放大到标题阈值后显示原生 `customCallout` 标题气泡，再放大到图片阈值后气泡扩展为“首张/封面图片缩略图 + 标题”；缩小时按相反顺序收缩。同步修正 `getMarkerDisplayMode` 的缩放方向，按微信地图 scale 越大展示内容越丰富处理，并在 `regionchange` 中同步手势缩放后的 `mapScale`。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移；使用现有 `cover_photo_url`、`label_min_zoom`、`preview_min_zoom` 与 `preview_enabled` 字段。
- 验证：先运行 `npm run test -- --run tests/pages/map-page.test.ts -t "marker display|POIs|custom marker bubbles"`，新增/调整测试在旧实现下失败；实现后该命令通过（4 passed）。随后 `npm run test -- --run tests/pages/map-page.test.ts` 通过（47 passed）；`npm run type-check` 通过；`npm run test -- --run` 通过（24 files，158 tests）；`npm run build:mp-weixin` 通过；`rg -l 'customCallout|marker-callout-thumb|marker-id|slot="callout"|anchor:\{x:\.5,y:1\}' dist/build/mp-weixin src/pages/index/index.vue` 确认源码和小程序产物保留 `customCallout`、callout slot、`marker-id`、缩略图样式与锚点；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机手动验证底图 POI 图标与地标 SVG 的视觉重合度、不同缩放档位下气泡展开/收缩动画观感。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后复测：点击底图 POI、搜索外部 POI，确认地标 SVG 落在 POI 图标位置；放大/缩小自定义 marker，确认标题气泡与图片缩略图按档位展开和收缩。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：修复地图平滑回中两个交互问题。POI、marker、列表点位和任务详情跳转到地图的点位聚焦统一改为 `focusMapToPoint`，会先设置点位焦点缩放级别，再通过响应式中心点插值动画平滑移动到目标点位中心，不再依赖 `moveToLocation` 做任意点位移动。地图拖动结束后同步真实 `mapCenter`，拖出限制边界时仍平滑拉回；手动拖动后再次点击“我的位置”可以重新从当前视口平滑回到当前位置。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移。
- 验证：先运行 `npm run test -- --run tests/pages/map-page.test.ts -t "animated focused|manual map drags"`，新增回归测试在旧实现下失败；实现后该命令通过（2 passed）。随后 `npm run test -- --run tests/pages/map-page.test.ts` 通过（45 passed）；`npm run test -- --run` 通过（24 files，156 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；检查 `frontend/dist/build/mp-weixin/pages/index/index.wxml` 确认地图仍绑定 `scale="{{e}}"` 与 `show-location="{{j}}"`；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机手动验证动画观感；需要导入构建产物后实测 POI 连续切换、拖图后“我的位置”回中。
- 下一步：微信开发者工具导入 `frontend/dist/build/mp-weixin`，连续点击多个底图 POI，确认地图以点位为中心平滑移动并缩放；拖动地图后重复点击“我的位置”，确认每次都能回到当前位置。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：修复地图页“我的位置”冷启动定位过慢的问题。新增前端 `user-location` 服务，在小程序 `App Launch` 时预加载定位权限和当前位置，并在地图页挂载时订阅最新位置；接入微信小程序 `startLocationUpdate`/`onLocationChange` 实时更新当前位置，让原生 map 的 `show-location` 持续显示当前位置。地图页“我的位置”按钮改为只读取已缓存/实时位置，设置合适缩放级别并通过 `moveToLocation` 平滑回中，不再清空当前位置、不再触发 `getLocation`/授权请求、不再刷新点位列表。
- 修改文件：`frontend/src/services/user-location.ts`、`frontend/src/App.vue`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移；仅调整前端定位与地图视口交互。
- 验证：先运行 `npm run test -- --run tests/pages/map-page.test.ts -t "preloads realtime user location"` 观察到新增红线测试失败；实现后该测试通过。随后 `npm run test -- --run tests/pages/map-page.test.ts` 通过（44 passed）；`npm run test -- --run` 通过（24 files，155 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；检查 `frontend/dist/build/mp-weixin/pages/index/index.wxml` 确认 `<map>` 保留 `scale="{{e}}"` 与 `show-location="{{j}}"` 绑定；检查构建产物 `services/user-location.js` 确认包含 `startLocationUpdate`/`onLocationChange`；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机手动验证定位授权弹窗、实时位置蓝点刷新和按钮回中动画；这些依赖真实小程序运行环境。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后，在微信开发者工具中验证首次进入小程序即触发定位授权、进入地图页无需再次等待即可显示当前位置、移动设备位置后地图蓝点持续更新、点击“我的位置”只平滑回中并调整缩放。

### 2026-07-01

- 分支：main
- 模块：地图模块
- 完成：阅读最近提交和进度记录，确认 `3e85ea4 feat(map): add in-app navigation and point editing` 引入模拟导航 marker，`75b221b fix(map-tasks): refine mini program navigation and task status editing` 改为路线规划后自动沿线移动；按当前反馈移除地图页动态导航能力，保留绿色路线 polyline 和底部路线卡，不再创建 `simulatedNavigationMarker`，不再用 `setInterval` 推动点位沿路线移动，也不再在路线点上调用 `setUserLocation`；原生 POI 点击只更新底部点位卡和用 `moveToLocation` 平滑居中，不再把 POI 包成临时 marker，因此不会额外出现 `定位.svg` 自定义 pin。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移；仅调整地图页前端交互。
- 验证：先新增红灯测试确认旧实现仍包含 `startNavigationTracking`、`simulatedNavigationMarker`、`navigationSimulationTimer`，且 `handleNativePoiTap` 会写入 `mapPointMarkers.value`；实现后 `npm run test -- --run tests/pages/map-page.test.ts -t "static navigation route|native tencent poi"` 通过（2 passed）；`npm run test -- --run tests/pages/map-page.test.ts` 通过（44 passed）；`npm run test -- --run` 通过（24 files，155 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；构建产物 `dist/build/mp-weixin/pages/index/*` 中未检出 `simulatedNavigationMarker`、`startNavigationTracking`、`navigationSimulation`、`setInterval`；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机做视觉手动复测；原生地图自身 POI 图标仍由微信/腾讯地图底图控制，代码只移除本项目额外叠加的自定义定位 SVG。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后复测：点击底图 POI 只显示底部详情卡且不新增粉色定位 SVG；点击导航只显示绿色路线和路线卡，不出现沿线移动点。

- 分支：main
- 模块：地图模块
- 完成：按腾讯位置服务 WebService API 的 walking 路线规划文档复核实现，确认 `from/to` 为 `lat,lng`，`polyline` 的前两个值为真实纬经度浮点数，后续点为百万分之一度增量；修复后端此前把首点强转整数再整体除以 1,000,000 导致路线点落到异常坐标的问题；前端路线渲染不再把服务商返回路线点 clamp 到校园边界，避免错误点被挤到边界形成“奇怪目标点”，并显式把当前位置与用户选定目的地拼入展示路线，确保路线起终点稳定。
- 修改文件：`backend/app/modules/map/service.py`、`backend/tests/test_map_api.py`、`frontend/src/pages/index/index.vue`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口、字段、表或迁移；保留 `GET /api/v1/map/route/walking` 和点位导航接口既有响应结构。
- 验证：先新增官方格式 polyline 解码红灯测试，确认旧实现会把 `[30.22991, 115.0622, 1490, -700, ...]` 解成 `0.00003/0.000115` 附近异常点；实现后 `python -m pytest tests/test_map_api.py::test_parse_tencent_polyline_decodes_official_float_start_and_delta_points tests/test_map_api.py::test_map_navigation_returns_walking_route_geometry -q` 通过；真实调用腾讯 walking 路线返回 `provider=tencent`、`fallback=False`、距离 525m、路线点 31 个、首点 `115.062343,30.229471`、末点 `115.060883,30.233008`、步骤 4 条；`python -m pytest tests/test_map_api.py -q` 通过（15 passed）；`python -m pytest -q` 通过（79 passed）；`python -m ruff check .` 通过；`npm run test -- --run tests/pages/map-page.test.ts` 通过（44 passed）；`npm run test -- --run` 通过（24 files，155 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；构建产物 `frontend/dist/build/mp-weixin/pages/index/index.wxml` 确认保留 `polyline="{{g}}"` 绑定；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机做视觉手动复测；真实路线仍依赖腾讯路线服务 Key/Secret、配额、后端网络和小程序定位授权。
- 下一步：导入 `frontend/dist/build/mp-weixin`，用校内两个点位验证点击“导航”后目的地仍为所选点、地图出现绿色步行路线、底部卡显示距离/时间/首步说明，拖拽边界和 POI 平滑居中一并复测。

- 分支：main
- 模块：地图模块、文件上传模块
- 完成：修复已完成图片资产展示接口仍强依赖 COS 配置的问题，`/api/v1/files/assets/{asset_id}/variant` 与 `/content` 在签名器不可用时回退到数据库保存的变体 URL，避免历史图片因缺少腾讯云 COS 配置返回 65008；`GET /api/v1/map/init` 新增 `core_bounds`/`limit_bounds`，后端地图点查询默认裁在湖北师范大学外扩限制边界内；前端地图初始化消费后端边界，拖出限制范围后平滑回正；路线 polyline 改为小程序原生 map 稳定可见的纯色配置，并在路线点非空时调用原生 `includePoints` 适配视口；POI/marker 点击改为原生 `moveToLocation` 平滑居中；点位详情卡增加文本去重，隐藏与标题/分类/POI 重复的描述、位置和公共地点内容。
- 修改文件：`backend/app/api/v1/files.py`、`backend/app/modules/files/{dependencies.py,service.py}`、`backend/app/modules/map/service.py`、`backend/tests/{test_files_api.py,test_map_api.py}`、`frontend/src/api/map.ts`、`frontend/src/pages/index/{index.vue,map-page.ts}`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：`GET /api/v1/map/init` 的 `campus` 增加 `core_bounds` 与 `limit_bounds`；文件读取接口行为调整为 COS 签名不可用时使用已保存 URL；无新增表或迁移。
- 验证：先新增红灯测试并确认失败，覆盖 COS 配置缺失展示回退、地图初始化边界返回、路线 polyline 可见配置、POI/marker 平滑居中、详情卡去重和区域限制回弹；实现后 `python -m pytest tests/test_files_api.py tests/test_map_api.py -q` 通过（26 passed）；`python -m pytest -q` 通过（78 passed）；`python -m ruff check .` 通过；`npm run test -- --run tests/pages/map-page.test.ts` 通过（43 passed）；`npm run test -- --run` 通过（24 files，154 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机手动复测地图手势动画、路线视觉效果和 COS 真实对象访问；真实图片是否公开可访问仍取决于保存 URL 对应对象权限或 COS CDN 策略。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后在微信开发者工具逐项复测：图片展示、路线折线、POI 平滑居中、详情卡去重和湖北师范大学区域拖拽限制。

### 2026-06-30

- 分支：main
- 模块：地图模块、任务模块
- 完成：修复本轮反馈的 5 个地图交互问题：地图页选择筛选项后，后端返回的 marker 会同步映射为底部动态列表；任务详情“导航前往”改为 `focus_only` 跳地图，只居中并显示该任务点 marker，不再自动进入路线规划，也不会显示其他 marker；筛选项达到两个及以上时插入 `all`，纯任务筛选场景文案为“全部任务类型”；“我的位置”恢复原生 `show-location` 位置点并继续居中，同时保持移除 `include-points`，避免触发腾讯 GLJS `fitBounds` 空/异常点数组崩溃；`drawer.wxs` 为筛选层增加展开态最小顶部距离，防止抽屉展开后被 vConsole 或系统顶部 UI 遮挡。顺带修复动态 `task` 筛选在前端列表中无法匹配 `daily_task/emergency_task` 的问题。
- 修改文件：`backend/app/modules/map/service.py`、`backend/tests/test_tasks_api.py`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/src/pages/index/drawer.wxs`、`frontend/src/pages/tasks/detail.vue`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口或表；调整 `GET /api/v1/map/init` 的 `filter_options` 生成规则，两个及以上动态筛选项即返回 `all`，纯任务筛选时 `all` 文案为“全部任务类型”。
- 验证：先新增前后端红灯测试并确认失败，覆盖原生定位点、筛选层 WXS 位置、任务详情 focus-only、两个筛选项插入 all、筛选 marker 同步抽屉列表和混合完成/未完成任务筛选；实现后 `npm run test -- --run tests/pages/map-page.test.ts tests/pages/tasks-page.test.ts` 通过（58 passed）；`python -m pytest tests/test_tasks_api.py::test_map_init_and_points_use_dynamic_feeding_completion_filters -q` 通过；`npm run type-check` 通过；`python -m pytest tests/test_map_api.py tests/test_tasks_api.py -q` 通过（20 passed）；`python -m ruff check .` 通过；`npm run test -- --run` 通过（24 files，150 tests）；`npm run build:mp-weixin` 通过；`python -m pytest -q` 通过（76 passed）；构建产物 `frontend/dist/build/mp-weixin/pages/index/index.wxml` 已无 `include-points`，`show-location` 为运行时布尔绑定。
- 阻塞：未在微信开发者工具或真机手动复测；定位显示仍依赖小程序定位授权、开发者工具定位模拟或真机系统定位权限。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后复测：筛选下拉展开位置、未完成/完成/全部任务类型列表与 marker 同步、任务详情跳地图单点聚焦、“我的位置”授权后显示原生位置点且不再触发 `fitBounds` 报错。

- 分支：main
- 模块：地图模块
- 完成：定位并修复腾讯地图功能调用失败的三处根因：`frontend/src/manifest.json` 原先 `mp-weixin.appid` 为空，构建产物会变成 `touristappid`，现固定为授权 AppID；后端腾讯 WebService 请求原先只带 `key`，没有在启用 Secret Key 时生成 `sig`，现新增 `CATMAP_TENCENT_MAP_SECRET_KEY` 配置并按腾讯 WebService SN 规则签名；地图页移除原生 `map` 的 `include-points` 绑定，避免空数组触发腾讯 GLJS `fitBounds` 的 `Cannot read properties of undefined (reading 'lat')` 崩溃。已把本地忽略的 `backend/.env` 补齐腾讯地图 Key/Secret/Referer，运行中的后端需要重启后生效。
- 修改文件：`backend/app/core/config.py`、`backend/app/modules/map/service.py`、`backend/.env.example`、`backend/tests/test_map_api.py`、`frontend/src/manifest.json`、`frontend/src/pages/index/index.vue`、`frontend/tests/config/miniapp-config.test.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增业务接口或表；新增后端环境变量 `CATMAP_TENCENT_MAP_SECRET_KEY`；`GET /api/v1/map/*` 内部调用腾讯 WebService 时会在配置 Secret Key 后追加 `sig`。
- 验证：新增红灯测试并确认失败：小程序 AppID 为空、地图页仍绑定 `include-points`、腾讯 WebService 请求缺少 `sig`；实现后 `npm run test -- --run tests/pages/map-page.test.ts tests/config/miniapp-config.test.ts` 通过（39 passed）；`python -m pytest tests/test_map_api.py::test_tencent_webservice_request_signs_query_when_secret_is_configured -q` 通过；真实调用腾讯地点搜索返回 `status=0 / Success`；`npm run test -- --run` 通过（24 files，149 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过，构建产物 `project.config.json` 已为授权 AppID；`python -m pytest tests/test_map_api.py -q` 通过（14 passed）；`python -m ruff check .` 通过；`python -m pytest -q` 通过（76 passed）；`git diff --check` 通过。
- 阻塞：未在微信开发者工具或真机手动复测；腾讯开放平台调用统计可能有延迟；部署环境如果不是本机，还需要同步设置 `CATMAP_TENCENT_MAP_SECRET_KEY` 并重启后端。
- 下一步：重启后端，重新导入 `frontend/dist/build/mp-weixin`，确认开发者工具项目 AppID 为授权 AppID；再测试 POI 搜索/点击解析、附近地点推荐和步行路线，观察腾讯开放平台调用统计。

- 分支：main
- 模块：地图模块
- 完成：按最新反馈修复地图筛选与导航体验：筛选下拉层从 `300rpx` 下移到 `380rpx`，避开 vConsole/系统顶部浮层；原生 `map` 关闭 `show-location`，点击“我的位置”仍会定位并居中但不再额外显示定位 SVG/蓝点；`include-points` 只保留真实路线点，筛选 marker 时不再自动 fit 到点位中心；原生 POI 点击和后端解析结果都先校验湖北师范大学扩展边界，校外 POI 不再响应；`/map/init` 的筛选项按已发布点位动态生成，超过两个类型才插入“全部标记”；路线接口返回 `fallback=true` 时前端不再绘制当前位置到目标点的直线假路线，只定位到目的地并提示路线规划暂不可用。
- 修改文件：`backend/app/modules/map/service.py`、`backend/tests/test_map_api.py`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增接口或表；调整 `GET /api/v1/map/init` 的 `filter_options` 生成规则，前端兼容后端返回的动态筛选项。
- 验证：先新增前后端回归测试并观察到筛选层位置、`show-location`、`include-points`、边界外 POI、动态“全部标记”断言失败；实现后 `npm run test -- --run tests/pages/map-page.test.ts` 通过（37 passed）；`python -m pytest tests/test_map_api.py -q` 通过（13 passed）；`python -m ruff check .` 通过；`python -m pytest -q` 通过（75 passed）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run test -- --run` 通过（23 files，147 tests）。
- 阻塞：未在微信开发者工具或真机手动复测；真实腾讯步行路线仍依赖腾讯地图 Key、步行路线/地点搜索配额、合法域名/Referer 配置和后端外网访问。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后，用真机或开发者工具复测筛选下拉层位置、筛选不移动视口、校外 POI 点击无响应、“我的位置”居中且不额外画点、腾讯路线可用时显示真实折线、腾讯路线不可用时不显示直线假路线。

- 分支：codex/feature-tencent-map-poi
- 模块：地图模块、暑假喂食任务模块、管理员选点页
- 完成：后端新增腾讯地图配置、POI 搜索/点击解析、附近 POI 推荐、临时步行路线接口；`map_points` 增加腾讯 POI 关联字段并通过任务发布/编辑保存；地图页启用原生 POI 点击、搜索结果和公共 POI 均展示底部卡片并用腾讯路线预览；管理员任务选点页点击地图后推荐附近 POI，支持关联、换一个、不关联，喂食点名称和位置补充说明不再有默认值且提交必填；任务详情展示关联公共地点。
- 修改文件：`backend/app/{core/config.py,api/v1/{map.py,admin_map.py},modules/{map, tasks}/**}`、`backend/alembic/versions/20260630_0010_add_tencent_map_poi_fields.py`、`backend/tests/{test_map_api.py,test_tasks_api.py}`、`frontend/src/api/{map,admin-map,tasks}.ts`、`frontend/src/pages/{index,admin/tasks,tasks}/**`、`frontend/tests/{api,pages}/**`、`docs/plans/2026-06-30-tencent-map-poi.md`、`docs/接口文档/{地图模块_接口设计文档.md,暑假喂食任务模块_接口文档.md}`、`docs/库表文档/{地图模块_库表设计说明.md,暑假喂食任务模块_库表设计文档.md}`、`docs/开发进度.md`
- API 或表变更：新增 `GET /api/v1/map/poi/resolve`、`GET /api/v1/map/poi/nearby`、`GET /api/v1/map/route/walking`；`GET /api/v1/map/init` 返回 `tencent_config`；`GET /api/v1/map/search` 外部 POI 切换为腾讯；`summary/navigation/task detail` 返回 `associated_poi`；迁移 `20260630_0010_add_tencent_map_poi_fields.py`。
- 验证：先新增后端/前端失败测试，确认腾讯配置、搜索、解析、路线、任务 POI 保存、前端原生 POI tap、选点页必填和详情展示断言失败；实现后 `python -m pytest` 在 `backend` 通过（74 passed）；`python -m ruff check .` 在 `backend` 通过；`python -m pytest tests\test_map_api.py tests\test_tasks_api.py` 在 `backend` 通过（18 passed）；`npm test` 在 `frontend` 通过（23 files，144 tests）；`npm test -- tests/api/map.test.ts tests/api/tasks.test.ts tests/pages/map-page.test.ts tests/pages/tasks-page.test.ts` 通过（4 files，67 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具或真机手动复测；腾讯位置服务真实调用依赖 Key 服务配额、域名/Referer 配置和后端外网访问。
- 下一步：在微信开发者工具导入 `frontend/dist/build/mp-weixin` 后复测原生 POI 点击、校园范围搜索、管理员选点推荐/取消关联、任务详情关联 POI 展示和路线预览。

- 分支：main
- 模块：个人中心模块、猫咪库模块、前端素材与原型整理
- 分支：main
- 模块：个人中心模块、猫咪库模块、前端素材与原型整理
- 完成：将此前工作树恢复出的新文档、页面原型目录、SVG 素材和测试图片纳入 main；“我的”页统计卡改用用户页提供的任务/进行中 SVG，菜单行改用设置、通知、帮助与反馈 SVG，未提供专用素材的观察记录和管理员入口继续保留清晰占位；猫咪库“毕业”统计项改用猫咪库素材里的星球 SVG，并同步把统计项标记为有图标。
- 修改文件：`frontend/src/pages/profile/index.vue`、`frontend/src/pages/cats/{index.vue,cats-page.ts}`、`frontend/tests/pages/{profile-page,cats-page}.test.ts`、`frontend/素材/svg/{用户页,猫咪库}/**`、`frontend/页面原型/**`、`docs/{Prompt,接口文档,库表文档,模块功能}/**`、`test/img/**`、`docs/开发进度.md`
- API 或表变更：无。
- 验证：先新增/调整前端断言并观察到用户页 SVG 引用、猫咪库星球 SVG 和毕业统计 `has_icon` 断言失败；实现后 `npm test -- --run tests/pages/profile-page.test.ts tests/pages/cats-page.test.ts` 通过（2 files，18 tests）；`npm test -- --run` 通过（23 files，140 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具里手动对照“我的”页和猫咪库页截图；用户页观察记录和管理员入口暂无专用 SVG，本轮未使用其它素材替代。
- 下一步：导入 `frontend/dist/build/mp-weixin` 后对照用户页原型和猫咪库原型检查图标尺寸、颜色和行高。

- 分支：codex/feature-map-navigation-admin-edit
- 模块：地图模块、暑假喂食任务模块、管理员任务编辑
- 完成：任务编辑页最后新增“任务完成状态”选择并调用管理员状态更新接口；“我的位置”改为申请 scope.userLocation 后使用 wx.getLocation 获取 gcj02 坐标，地图显示位置点并把视口中心移动到定位点，不再弹出当前位置坐标提示；地图页导航文案统一为“导航”，点击后直接在底部动态内容区展示路线并自动启动当前位置沿路线移动，不再出现额外“模拟导航”按钮；原生 map 的 marker、include-points、路线点、搜索 POI 和编辑坐标均增加有限数字校验与湖北师范大学校园扩展边界裁剪，避免 QQ map fitBounds 收到 undefined/NaN 坐标；任务详情页“导航前往”写入待导航目标并 switchTab 到地图页，由地图页消费后执行同一套导航逻辑。
- 修改文件：`frontend/src/api/tasks.ts`、`frontend/src/pages/admin/tasks/create.vue`、`frontend/src/pages/index/{index.vue,map-page.ts}`、`frontend/src/pages/tasks/{detail.vue,task-page.ts}`、`frontend/tests/api/tasks.test.ts`、`frontend/tests/pages/{map-page,tasks-page}.test.ts`、`docs/开发进度.md`
- API 或表变更：无数据库表变更；前端新增调用既有 `PATCH /api/v1/admin/tasks/{task_id}/status` 的 `updateSummerFeedingTaskStatus` helper。
- 验证：先新增前端回归测试并观察到任务状态编辑、详情页导航联动、微信定位函数、坐标过滤和 pending navigation 断言失败；实现后 `npm test -- --run tests/api/tasks.test.ts tests/pages/tasks-page.test.ts tests/pages/map-page.test.ts` 通过（3 files，58 tests）；`npm run type-check` 通过；`npm test -- --run` 通过（23 files，138 tests）；`npm run build:mp-weixin` 通过；`pytest backend/tests/test_tasks_api.py -q` 通过（6 passed）。
- 阻塞：未在微信开发者工具或真机手动复测；真实定位仍依赖小程序定位权限、开发者工具定位模拟配置和设备系统定位权限。
- 下一步：在微信开发者工具导入 `frontend/dist/build/mp-weixin`，复测“我的位置”授权/定位、公共 POI 搜索、任务详情跳地图导航、路线移动展示、校园边界拖动回弹和管理员编辑任务状态。

- 分支：codex/feature-map-navigation-admin-edit
- 模块：地图模块、暑假喂食任务模块、管理员任务编辑
- 完成：修复微信开发者工具反馈的地图与任务编辑体验：任务详情页管理员可在标题右侧进入“编辑喂食任务”，复用发布任务页加载并编辑标题、说明、日期、位置、物资、任务点图片和路线说明；新增 `GET /api/v1/admin/tasks/{task_id}` 返回可编辑任务详情，普通/管理员任务详情按角色返回 `can_admin_edit`；任务详情加载失败时增加一次短重试和手动“重新加载”。地图页点击导航后不再显示地图浮层，改为底部动态内容区展示路线、距离、时间、首步说明、模拟导航/停止/结束；“我的位置”补充小程序定位权限声明、授权请求和校园中心兜底；地图点位不再通过抽屉按钮编辑，管理员长按 marker 或长按靠近 marker 的地图位置进入编辑模式，并可拖动地图上的编辑手柄更新坐标后保存。
- 修改文件：`backend/app/api/v1/{tasks,admin_tasks}.py`、`backend/app/modules/tasks/service.py`、`backend/tests/test_tasks_api.py`、`frontend/src/api/tasks.ts`、`frontend/src/manifest.json`、`frontend/src/pages/admin/tasks/create.vue`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/tasks/detail.vue`、`frontend/tests/api/tasks.test.ts`、`frontend/tests/pages/{map-page,tasks-page}.test.ts`、`docs/开发进度.md`
- API 或表变更：新增 `GET /api/v1/admin/tasks/{task_id}`；`GET /api/v1/tasks/{task_id}` 的 `actions.can_admin_edit` 按当前用户角色返回；无数据库表结构和迁移变更。
- 验证：先新增回归测试并观察到管理员可编辑详情 405、前端任务编辑 API 缺失、地图导航浮层/编辑按钮/定位兜底和任务详情编辑/重试断言失败；实现后 `pytest -q` 通过（73 passed）；`ruff check .` 通过；`npm test -- --run` 通过（23 files，133 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具或真机手动复测；地图编辑拖拽依赖微信小程序 `MapContext.fromScreenLocation`，若低基础库不支持会退回当前地图中心坐标再保存。
- 下一步：在微信开发者工具导入 `frontend/dist/build/mp-weixin`，用管理员账号验证任务详情编辑入口、发布页编辑回填与图片删除/新增、地图页导航抽屉、模拟导航、“我的位置”权限弹窗和长按/拖拽点位保存。

- 分支：codex/feature-map-navigation-admin-edit
- 模块：地图模块、管理员点位编辑
- 完成：后端地图搜索支持 `include_external=true` 时合并高德 Web Service POI；点位导航接口可接收当前位置并返回步行路线点串、距离、耗时和步骤，外部服务失败时保留直线兜底；新增 `/api/v1/admin/map/points/{point_id}`、`PATCH /points/{point_id}`、`PATCH /points/{point_id}/location` 管理员点位详情/编辑/坐标更新接口。前端地图页移除 `uni.openLocation` 外跳，改为当前原生 map 内绘制 polyline；新增导航状态浮层和模拟导航 marker 移动；搜索请求携带 `include_external=true` 并把外部 POI 作为临时 marker/卡片；管理员点位卡片显示“编辑点位”，并新增点位编辑页；管理员选中点位后长按地图进入位置编辑模式，保存时更新点位坐标。
- 修改文件：`backend/app/api/v1/{map,admin_map,router}.py`、`backend/app/modules/map/service.py`、`backend/app/core/config.py`、`backend/.env.example`、`backend/tests/test_map_api.py`、`frontend/src/api/{map,admin-map}.ts`、`frontend/src/pages/index/{index.vue,map-page.ts}`、`frontend/src/pages/admin/map-point/edit.vue`、`frontend/src/pages.json`、`frontend/tests/api/map.test.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/plans/2026-06-30-map-navigation-admin-edit*.md`、`docs/开发进度.md`
- API 或表变更：`GET /api/v1/map/search` 新增 `include_external`；`GET /api/v1/map/points/{point_id}/navigation` 新增 `from_lng/from_lat` 并返回 `route`；新增管理员地图点位接口；无数据库表结构变更。
- 验证：先新增后端/前端回归测试并观察到外部 POI、路线 geometry、管理员点位接口、前端外跳导航和编辑入口断言失败；实现后 `py -3.11 -m pytest tests/test_map_api.py -q` 通过（11 passed）；`py -3.11 -m pytest -q` 通过（72 passed）；`py -3.11 -m ruff check .` 通过；`npm run test -- --run tests/api/map.test.ts tests/pages/map-page.test.ts` 通过（2 files，35 tests）；`npm run test -- --run` 通过（23 files，127 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具真机实测；高德外部 POI 和步行路线依赖 `CATMAP_AMAP_WEB_SERVICE_KEY` 或当前 Web Key 具备 Web Service 权限，生产环境还需要确认服务配额与网络访问；微信原生 map 没有稳定公开 marker draggable 字段，本分支采用长按进入编辑模式后移动地图中心/编辑 marker 保存坐标的兼容方案。
- 下一步：在微信开发者工具导入 `frontend/dist/build/mp-weixin`，用管理员账号测试搜索教学楼等高德 POI、点击任务点本页路线规划、模拟导航移动、点位编辑页保存、长按点位后更新坐标。

### 2026-06-29

- 分支：codex/fix-task-map-upload-carousel-cache
- 模块：暑假喂食任务模块、地图模块、文件上传模块
- 完成：放宽任务点图片上传原图像素上限至 5000 万像素，并同步 `.env.example` 和本地 `.env`；任务详情顶部图片改为 5 秒自动轮播、支持手动滑动，并在图片加载失败时回退占位；任务列表增加前端内存缓存，切回任务 tab 时先显示缓存再静默刷新，完成投喂后清空缓存；地图初始化新增后端动态 `filter_options`，前端下拉菜单默认“无标记”，按后端返回展示未完成/完成喂食任务筛选；地图点查询支持 `filter_key=feeding_pending/feeding_completed` 并返回 `feeding_status`，前端用失败/完成任务 SVG 显示 marker；地图 summary 的“查看详情”按钮已跳转任务详情页；筛选菜单开关改由 Vue 控制，WXS 只做样式同步，避免 `this.$vm[e] is not a function`；筛选浮层下移到 `300rpx`，并将 marker 坐标传给原生 map 的 `include-points`。
- 修改文件：`backend/app/core/config.py`、`backend/.env.example`、`backend/app/api/v1/map.py`、`backend/app/modules/map/service.py`、`backend/tests/test_files_api.py`、`backend/tests/test_tasks_api.py`、`frontend/src/api/map.ts`、`frontend/src/pages/index/{index.vue,map-page.ts,filter-menu.wxs}`、`frontend/src/pages/tasks/{index.vue,detail.vue,task-list-cache.ts}`、`frontend/tests/api/map.test.ts`、`frontend/tests/pages/{map-page,tasks-page,tasks-cache}.test.ts`、`docs/开发进度.md`
- API 或表变更：`GET /api/v1/map/init` 响应新增 `filter_options`；`GET /api/v1/map/points` 新增可选查询参数 `filter_key`；无数据库表结构变更。
- 验证：先新增回归测试并观察到上传高分辨率图、地图动态筛选、前端轮播/缓存/筛选跳转断言失败；实现后 `pytest backend/tests/test_files_api.py backend/tests/test_tasks_api.py backend/tests/test_map_api.py` 通过（22 passed）；`npm run test -- --run tests/pages/tasks-page.test.ts tests/pages/tasks-cache.test.ts tests/pages/map-page.test.ts tests/api/map.test.ts tests/api/tasks.test.ts` 通过（5 files，50 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：COS 403 仍按前次判断需要检查云端对象权限、签名/CDN 鉴权或旧数据直连 URL，本分支未改 COS 权限；未在微信开发者工具真机界面手动复测。
- 下一步：重启后端使 `.env` 新像素上限生效，重新导入或刷新 `frontend/dist/build/mp-weixin`，在微信开发者工具验证任务图上传、详情轮播、地图筛选、marker 坐标和“查看详情”跳转。

- 分支：codex/fix-wechat-task-detail-drawer
- 模块：地图模块、暑假喂食任务模块、文件访问前端兼容
- 完成：修复微信小程序运行时缺少 `URLSearchParams` 导致任务详情页无法加载的问题；地图页进入时不再默认启用原生定位蓝点，避免页面进入阶段触发定位超时；地图动态任务行移除中间“喂食任务”标签，仅保留右侧状态标签，并收紧标题与任务图标间距；动态抽屉恢复近全屏展开能力，同时在最终展开态保留顶部安全距离；任务 tab 移除发布入口，发布仍保留在管理员入口。
- 修改文件：`frontend/src/api/files.ts`、`frontend/src/api/me.ts`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/drawer.wxs`、`frontend/src/pages/tasks/index.vue`、`frontend/tests/api/files.test.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/tasks-page.test.ts`、`docs/开发进度.md`
- API 或表变更：无新增 API、无数据库变更。
- 验证：先新增/调整回归测试并观察到 `URLSearchParams`、地图 show-location、抽屉高度、地图任务行和任务页发布入口断言失败；实现后 `npx vitest run --config vitest.config.ts tests/api/files.test.ts tests/pages/map-page.test.ts tests/pages/tasks-page.test.ts` 通过（3 files，38 tests）；`npm run type-check` 通过；`npm run test` 通过（22 files，116 tests）；`npm run build:mp-weixin` 通过。
- 阻塞：COS 图片 403 本分支先不修代码；当前报错 URL 仍是 `https://cat-map-<cos-app-id>.cos.ap-guangzhou.myqcloud.com/.../thumb_md.jpg` 这种直连 COS 地址，说明云端对象权限/签名/CDN 鉴权或旧数据裸 URL 路径仍需在云控制台和数据链路中核对。
- 下一步：在微信开发者工具重新导入或刷新 `frontend/dist/build/mp-weixin`，复测页面进入、地图抽屉拖动、地图动态任务行、任务 tab 和任务详情页加载。

- 分支：codex/fix-wechat-task-map-polish
- 模块：暑假喂食任务模块、地图模块、个人中心、文件访问。
- 完成：修复微信开发者工具实测反馈：任务页发布按钮移出右上胶囊遮挡区；发布页日期选择由原生三列 picker 改为底部多选日历，确定后批量写入日期；任务上传图片和详情图优先使用 `/api/v1/files/assets/{asset_id}/content` 展示地址，后端该地址会跳转到 COS 签名 URL，避免私有 COS 403；地图抽屉移除 WXS 对不存在 `onDrawerProgressChange` 的调用并把最大展开进度限制为 2；任务列表状态 pill 按当前执行日显示“已完成”；“我的”页增加 dashboard 请求防重和短缓存，降低切 tab 时的重复请求卡顿。
- 修改文件：`backend/app/api/v1/files.py`、`backend/app/modules/files/{storage,service,presets}.py`、`backend/tests/test_files_api.py`、`frontend/src/api/files.ts`、`frontend/src/pages/admin/tasks/create.vue`、`frontend/src/pages/index/{index.vue,drawer.wxs}`、`frontend/src/pages/profile/index.vue`、`frontend/src/pages/tasks/{index.vue,detail.vue,task-page.ts}`、`frontend/tests/api/files.test.ts`、`frontend/tests/pages/{tasks-page,map-page,profile-page}.test.ts`。
- API 或表变更：新增 `GET /api/v1/files/assets/{asset_id}/content` 图片展示跳转接口；`GET /api/v1/files/assets/{asset_id}/variant` 改为返回签名 URL；无数据库迁移。
- 验证：先新增/调整回归测试并观察到前端 8 个目标断言失败、后端文件测试 2 个目标断言失败；实现后 `npx vitest run --config vitest.config.ts tests/api/files.test.ts tests/pages/tasks-page.test.ts tests/pages/map-page.test.ts tests/pages/profile-page.test.ts` 通过（4 files，43 tests）；`py -3.11 -m pytest backend/tests/test_files_api.py -q` 通过（9 passed）；`npm run type-check` 通过；`py -3.11 -m pytest -q` 在 `backend` 通过（66 passed）；`npm run build:mp-weixin` 通过。
- 阻塞：SharedArrayBuffer deprecation 和 `WAWorker reportRealtimeAction:fail not support` 属于微信开发者工具/Chromium 环境提示，本分支无业务代码可消除；仍需在微信开发者工具导入新构建产物复测 9 条实测反馈。
- 下一步：重启/确认后端服务后，在微信开发者工具重新导入 `frontend/dist/build/mp-weixin`，复测“我的”页、任务发布多选日期、图片上传/详情展示、地图抽屉拖动和完成任务后列表刷新状态。

- 分支：codex/feature/summer-feeding-tasks
- 模块：暑假喂食任务模块、地图模块、文件上传前端接入。
- 开始检查：已阅读 `AGENTS.md`、项目说明、库表总览、接口规范、暑假喂食任务功能/接口/库表文档、地图模块功能说明，并查看任务模块原型 `发布任务.png`、`发布任务-日期选择.png`、`发布任务-地图选点.png`、`任务详情.png` 和地图首页原型。本次只做暑假喂食任务发布、地图可见任务点、任务列表/详情和完成投喂基础闭环；导航、拖拽更新点位、复杂指派和后期任务目录不在本分支范围。
- 完成：后端新增暑假喂食任务模型、迁移、管理员发布/编辑/状态接口、成员任务列表/详情/完成投喂接口；发布任务会写入 `map_points` 并通过地图接口返回真实 `task_id` 和 feeding marker 摘要。前端新增任务 API、图片上传 API、任务列表、任务详情、管理员发布页和地图选点页；发布表单包含标题、任务说明、时间、位置、所需物资（默认猫粮、水）、任务点图片、路线说明（默认空）；地图首页日常任务筛选包含 feeding 点；喂食任务 marker 使用 `完成任务.svg`/`失败任务.svg`，已分别改为绿色/红色。
- 修改文件：`backend/app/modules/tasks/**`、`backend/app/api/v1/{tasks,admin_tasks}.py`、`backend/app/modules/map/service.py`、`backend/alembic/versions/20260629_0009_create_summer_feeding_task_tables.py`、`backend/tests/test_tasks_api.py`、`frontend/src/api/{tasks,files}.ts`、`frontend/src/pages/tasks/**`、`frontend/src/pages/admin/tasks/**`、`frontend/src/pages/index/**`、`frontend/素材/svg/地图点/{完成任务,失败任务}.svg`、`frontend/tests/**`、`docs/plans/2026-06-29-summer-feeding-tasks*.md`、`docs/开发进度.md`
- API 或表变更：新增 `POST /api/v1/admin/tasks/summer-feeding`、`GET /api/v1/admin/tasks`、`PATCH /api/v1/admin/tasks/{task_id}`、`PATCH /api/v1/admin/tasks/{task_id}/status`、`GET /api/v1/tasks`、`GET /api/v1/tasks/{task_id}`、`POST /api/v1/tasks/{task_id}/checkins`；地图 `GET /api/v1/map/points`、summary、search、bottom-content 对任务点返回真实任务业务信息；前端接入现有 `POST /api/v1/files/images`。新增 `tasks`、`task_execution_dates`、`task_photos`、`task_checkins`、`task_checkin_photos`、`task_activity_logs`。
- 迁移：新增 `20260629_0009_create_summer_feeding_task_tables.py`，并补充 `task_feeding` marker 配置。
- 验证：后端先新增任务契约测试并观察到 404 红灯；实现后 `py -3.11 -m pytest tests/test_tasks_api.py tests/test_map_api.py -q` 通过（11 passed）；`py -3.11 -m ruff check .` 通过；`py -3.11 -m pytest -q` 通过（65 passed）。前端先新增任务/API/地图断言并观察到缺模块、缺路由、feeding 筛选和 SVG 断言失败；实现后 `npm run test -- --run tests/api/tasks.test.ts tests/api/files.test.ts tests/pages/tasks-page.test.ts tests/pages/admin-page.test.ts tests/pages/map-page.test.ts` 通过（5 files，38 tests）；`npm run type-check` 通过；`npm run test -- --run` 通过（22 files，108 tests）；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具或真机手动验收；文件上传 URL 是否可匿名访问仍依赖当前 COS/本地存储策略；任务详情中的导航按钮按本次范围仅提示后续接入。
- 下一步：用微信开发者工具导入 `frontend/dist/build/mp-weixin`，以管理员账号发布一条喂食任务并确认地图日常任务筛选出现任务点；随后再做任务导航、点位拖拽更新和更完整的任务管理/通知联动。

### 2026-06-28

- 分支：codex/wechat-device-layout-map-polish
- 模块：地图模块、猫咪库模块、个人中心模块、缺省页组件。
- 开始检查：按微信真机反馈复核地图页、猫咪库页、缺省页和“我的”页标题高度；已对照项目说明、库表总览、接口规范、地图模块文档、猫咪库更新文档、个人中心接口文档，以及 `frontend/页面原型/首页-常规状态.png`、`frontend/页面原型/猫咪库.png`、`frontend/页面原型/我的.png`、`frontend/页面原型/数据为空.png`。本次只调整前端页面与小程序地图实现，不改后端 API、数据库表或迁移。
- 完成：全局页面标题基准下移到“我的”页高度并统一缩小标题/副标题/标题图标；地图页同步下移并压缩地图展示区、筛选层和底部动态抽屉高度；地图页移除 H5 高德 Web SDK、AMap REST POI 搜索和 Web 地图容器，改为微信小程序原生 `<map>`、`uni.getLocation` 和 `uni.openLocation`；猫咪库标题右侧图标替换为 `寿司.svg`；“我的”页标题图标移到标题右侧并贴近标题；缺省页标题区域复用全局标题变量。
- 修改文件：`frontend/src/App.vue`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/cats/index.vue`、`frontend/src/pages/profile/index.vue`、`frontend/src/components/DefaultState.vue`、`frontend/tests/styles/global-font.test.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/tests/pages/cats-page.test.ts`、`frontend/tests/pages/profile-page.test.ts`、`frontend/tests/components/default-state.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：先新增/调整前端约束测试并观察到标题变量、猫咪库图标、我的页标题行、缺省页标题、原生地图路径等断言失败；实现后 `npm test -- --run tests/styles/global-font.test.ts tests/pages/cats-page.test.ts tests/pages/profile-page.test.ts tests/components/default-state.test.ts tests/pages/map-page.test.ts` 通过（5 files，45 tests）；`npm run type-check` 通过；`npm test` 通过（19 files，94 tests）；`npm run build:mp-weixin` 通过；`frontend/dist/build/mp-weixin` 目录大小约 1.88MB，低于 4MB 真机调试限制。
- 阻塞：未在微信开发者工具真机上复测最终视觉位置；微信小程序原生 map 的腾讯地图标识和版权属于平台地图组件展示内容，代码侧无法可靠隐藏，也不建议用覆盖层遮挡。
- 下一步：在微信开发者工具重新导入/刷新 `frontend/dist/build/mp-weixin`，真机检查地图页标题与地图区域、猫咪库标题和寿司图标、缺省页标题、“我的”页标题图标，以及“我的位置”和导航权限弹窗。

- 分支：codex/login-agreement-backend-state
- 模块：登录页与鉴权后端。
- 开始检查：根据反馈复核 `backend/app/modules/auth/service.py`，确认旧逻辑在查询账号前直接校验 `agree_terms`，无法利用 `users.must_change_password` 区分首登与非首登；沿用已读鉴权接口/库表文档和登录页原型。本次涉及 API 为 `POST /api/v1/auth/login`，涉及表为现有 `users/auth_captchas/user_profiles`，不新增字段、不新增迁移；最小切片为后端用 `must_change_password` 决定是否要求协议，前端不再本地硬拦未勾选协议。
- 完成：后端登录流程调整为验证码、账号、状态、锁定、密码校验通过后，仅当 `user.must_change_password = true` 且 `agree_terms = false` 时返回 `40005`；已完成首次改密的账号可不重复勾选协议登录。前端保留按账号本地自动勾选体验，但移除“未勾选协议”本地拦截，让后端统一决定是否需要协议。
- 修改文件：`backend/app/modules/auth/service.py`、`backend/tests/test_auth_api.py`、`frontend/src/pages/login/index.vue`、`frontend/tests/pages/login-agreement.test.ts`、`docs/接口文档/鉴权模块_接口文档.md`、`docs/库表文档/鉴权模块_库表设计文档.md`、`docs/开发进度.md`
- API 或表变更：登录接口业务规则调整；无新增接口、无新增字段。
- 迁移：无。
- 验证：先新增后端回归测试 `test_returning_login_allows_missing_agreement_after_password_changed` 并观察到 400 失败；新增前端断言并观察到登录页仍本地提示“请先勾选协议”失败；实现后两个红灯用例均通过。`py -3.11 -m pytest tests\test_auth_api.py -q` 通过（12 passed）；`py -3.11 -m pytest -q` 通过（58 passed）；`py -3.11 -m ruff check .` 通过；`npm run test -- tests/pages/login-agreement.test.ts tests/api/auth.test.ts tests/stores/user.test.ts` 通过（3 files，13 tests）；`npm run test` 通过（17 files，77 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过；`git diff --check` 通过。
- 阻塞：未在微信开发者工具中手动验证真实账号首登/非首登流程。
- 下一步：手动验收时分别用 `must_change_password=true` 和 `false` 的账号覆盖协议勾选行为。

- 分支：codex/login-agreement-first-login
- 模块：登录页与鉴权前端。
- 开始检查：已阅读 `docs/校园猫协地图任务系统_项目说明文档.md`、`docs/校园猫协地图任务系统_库表设计说明.md`、`docs/接口文档/接口设计规范文档.md`、`docs/接口文档/鉴权模块_接口文档.md`、`docs/库表文档/鉴权模块_库表设计文档.md`，并查看 `frontend/页面原型/登录页.png`；涉及 API 为现有 `POST /api/v1/auth/login`，涉及表为现有 `users/auth_captchas/user_profiles` 但本次无表结构修改；上游依赖为前端鉴权 API 封装和 Pinia 用户状态；最小切片为按账号本地记住已同意协议并自动勾选；本分支不新增后端协议审计表、不做跨设备协议状态同步、不改变登录页视觉版式。
- 完成：登录页新增按账号记住协议勾选逻辑。新账号首次登录仍需手动勾选；登录成功后记录该喵喵号/学号；后续输入同一账号时自动勾选，并继续按既有契约向后端提交 `agree_terms: true`。
- 修改文件：`frontend/src/pages/login/index.vue`、`frontend/src/pages/login/agreement.ts`、`frontend/src/constants/storage.ts`、`frontend/tests/pages/login-agreement.test.ts`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：先新增 `tests/pages/login-agreement.test.ts` 并观察到 2 个失败，覆盖协议记忆模块缺失和登录页未接入；实现后该测试通过。随后 `npm run test -- tests/pages/login-agreement.test.ts tests/api/auth.test.ts tests/stores/user.test.ts` 通过（3 files，12 tests）；`npm run type-check` 通过；`npm run test` 通过（17 files，76 tests）；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：未在微信开发者工具中手动输入同一账号复验自动勾选；本地记忆依赖当前设备/小程序本地存储，清缓存或换设备后仍需重新勾选一次。
- 下一步：在微信开发者工具中用真实账号验证“首次未勾选拦截 -> 勾选登录成功 -> 再回登录页输入同账号自动勾选”的完整交互。

- 分支：fix/map-filter-menu-clickable
- 完成：修复地图页筛选下拉菜单在地图缩放后无法点击的问题。根因是普通 `view/button` 菜单位于原生 `map` 容器内，缩放后可能被原生地图层吞掉点击事件；现改为地图安全的独立 `cover-view` 浮层，并由 `drawer.wxs` 同步浮层位置，避免视觉位置和点击区域分离。筛选项切换为更新后的命名地图点 SVG 素材，箭头改用 `箭头.svg`，通过 `filter-menu.wxs` 实现展开时菜单变宽、箭头右移并向上，收起时箭头回到左侧视觉重心并向下。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/drawer.wxs`、`frontend/src/pages/index/filter-menu.wxs`、`frontend/src/shime-uni.d.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/plans/2026-06-28-map-filter-menu-clickable.md`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：先新增回归测试并观察到 3 个失败，分别覆盖旧 SVG/CSS 箭头、普通 view 菜单仍在 map 容器内、缺少 WXS 动画；修复后 `npm test -- --run tests/pages/map-page.test.ts` 通过（19 tests）；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过；`npm run test` 通过（16 files，74 tests）；`git diff --check` 通过。
- 阻塞：未在微信开发者工具真机/模拟器里手动缩放地图后点击筛选菜单；本地没有可直接渲染 SVG 的截图工具，已通过 `箭头.svg` 路径坐标确认素材默认方向向上，WXS 收起态 `rotate(180deg)` 为向下、展开态 `rotate(0deg)` 为向上。
- 下一步：在微信开发者工具导入 `frontend/dist/build/mp-weixin`，手动执行地图缩放后点击筛选菜单、展开/收起箭头动画、抽屉拖动后筛选菜单点击区域一致性验收。

- 分支：feature/cats-library-list
- 完成：按新参考图优化猫咪库列表页；页面背景切换为加载页背景；猫咪库标题复用地图页标题位置/字号变量并替换透明背景萌猫 SVG；统计卡调整为在档猫咪、正常在校、待领养、已领养、毕业五项，底部居中展示“当前已绝育 xx 只流浪喵”并突出数量；筛选卡默认展示“全部”，缩小筛选标签、下拉值、箭头和清除筛选图标；列表支持触底按页懒加载并保留图片 lazy-load；后端统计接口补充 `adopted_cats`、`deceased_cats` 字段，`deceased` 展示为“毕业”。
- 修改文件：`AGENTS.md`、`backend/app/modules/cats/service.py`、`backend/tests/test_cats_api.py`、`frontend/src/App.vue`、`frontend/src/api/cats.ts`、`frontend/src/pages/cats/**`、`frontend/src/pages/index/index.vue`、`frontend/素材/svg/猫咪库/**`、`frontend/tests/**`、`docs/开发进度.md`
- API 或表变更：`GET /api/v1/cats/stats` 响应新增 `adopted_cats`、`deceased_cats`；无表结构变更、无新迁移。
- 验证：本地 H5 使用 390x844 手机视口截图核对统计/搜索/筛选区域；`py -3.11 -m pytest -q` 通过（60 passed）；`py -3.11 -m ruff check .` 通过；`npm run test` 通过（18 files / 85 tests）；`npm run type-check` 通过；`npm run build:h5` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：未在微信开发者工具真机/模拟器中验收原生 picker 展开态字号；本地截图为 H5 视口验证。截图联调用的 `preview_cats` 账号和预览猫数据已从本地开发库清理。
- 下一步：继续猫咪详情页和管理员猫咪录入流程，后续接入真实图片上传资产。

### 2026-06-27

- 分支：feature/cats-library-list
- 完成：阅读猫咪库功能、接口、库表更新文档和猫咪库原型图；新增猫咪库初始后端模型与迁移；新增成员端 `GET /api/v1/cats/stats`、`GET /api/v1/cats/filter-options`、`GET /api/v1/cats`；列表接口支持关键词、三段式筛选、排序、分页、别名摘要、展示标签和收藏标记；替换猫咪库 tab 占位页，实现统计卡、搜索、筛选、猫咪卡片列表、加载/空/错误/图片失败状态。
- 修改文件：`backend/app/modules/cats/**`、`backend/app/api/v1/cats.py`、`backend/app/api/v1/router.py`、`backend/alembic/env.py`、`backend/alembic/versions/20260627_0008_create_cat_archive_tables.py`、`backend/tests/{conftest.py,test_cats_api.py}`、`frontend/src/api/cats.ts`、`frontend/src/pages/cats/**`、`frontend/src/pages/index/index.vue`、`frontend/tests/api/cats.test.ts`、`frontend/tests/pages/cats-page.test.ts`、`docs/plans/2026-06-27-cats-library-list.md`、`docs/开发进度.md`
- API 或表变更：新增猫咪库成员端统计、筛选项和列表接口；新增 `cats`、`cat_aliases`、`cat_photos`、`cat_map_points`、`cat_observation_records`、`cat_health_records`、`cat_favorites`。
- 迁移：新增 `20260627_0008_create_cat_archive_tables.py`。
- 验证：先运行 `py -3.11 -m pytest tests\test_cats_api.py::test_cats_empty_list_contract -q` 观察到 404 红灯；实现后通过；新增数据筛选测试后观察到性格标签筛选红灯，改为服务层过滤后 `py -3.11 -m pytest tests\test_cats_api.py -q` 通过（3 passed）；前端先运行 `npm run test -- tests/api/cats.test.ts` 观察到缺少 `@/api/cats` 红灯，补 API 后通过；页面测试先观察到缺少 `cats-page` helper 红灯，补页面后通过；最终 `py -3.11 -m pytest -q` 通过（60 passed）；`py -3.11 -m ruff check .` 通过；读取主工作区 `.env` 后 `py -3.11 -m alembic upgrade head` 通过并执行 `20260627_0008`；`npm run test` 通过（18 files / 81 tests）；`npm run type-check` 通过；`npm run build:h5` 通过；`npm run build:mp-weixin` 通过。
- 构建修复：前端构建曾被地图页引用的缺失 SVG 阻塞，已将 `frontend/src/pages/index/index.vue` 的地图筛选图标改为素材目录中实际存在的 `全部.svg`、`猫咪点.svg`、`日常任务.svg`、`紧急任务.svg`、`地标.svg`、`物资点.svg`。
- 阻塞：尚未接入管理员新增/编辑猫咪流程，当前列表依赖数据库中已有猫咪数据；猫咪详情页尚未实现，列表点击目标路由已预留；未在微信开发者工具中做真机验收。
- 下一步：开发管理员四页式猫咪录入流程，接统一文件上传生成猫咪照片，再开发猫咪详情页和观察记录入口。

- 分支：feature/files-upload
- 完成：阅读 COS 图片上传功能、文件上传接口、图片上传库表优化文档；新增后端文件上传模块，支持上传配置、单图上传、批量上传、图片多尺寸变体生成、文件资产查询、按 scene/variant 获取 URL、临时图片绑定业务归属、逻辑删除；图片统一由 Pillow 修正 EXIF、去 EXIF、转 JPEG 并生成头像/普通图/路线图/高细节图预设；COS SDK 封装为对象存储适配器，测试中使用替身，不依赖真实 COS；新增 `file_assets`、`file_asset_variants` 表，并为 `user_profiles` 增加头像资产字段。
- 修改文件：`backend/app/api/v1/files.py`、`backend/app/api/v1/router.py`、`backend/app/modules/files/**`、`backend/app/modules/auth/models.py`、`backend/app/core/{config,errors}.py`、`backend/alembic/env.py`、`backend/alembic/versions/20260627_0007_create_file_asset_tables.py`、`backend/tests/{conftest.py,test_files_api.py}`、`backend/pyproject.toml`、`backend/.env.example`、`docs/plans/2026-06-27-files-upload.md`、`docs/开发进度.md`
- API 或表变更：新增 `/api/v1/files/config`、`POST /api/v1/files/images`、`POST /api/v1/files/images/batch`、`GET /api/v1/files/assets`、`GET /api/v1/files/assets/{asset_id}`、`GET /api/v1/files/assets/{asset_id}/variant`、`PATCH /api/v1/files/assets/{asset_id}/owner`、`DELETE /api/v1/files/assets/{asset_id}`；新增 `file_assets`、`file_asset_variants`；`user_profiles` 新增 `avatar_asset_id`、`avatar_thumb_url`。
- 迁移：新增并已执行 `20260627_0007_create_file_asset_tables.py`，当前 Alembic 版本为 `20260627_0007 (head)`。
- 验证：先运行 `py -3.11 -m pytest tests\test_files_api.py::test_get_file_upload_config_returns_image_limits -q` 观察到 404 红灯；实现配置接口后通过；新增上传主流程测试后先观察到缺少文件模块依赖红灯，补齐模块后文件测试通过；最终 `py -3.11 -m pytest tests\test_files_api.py -q` 通过（8 passed）；`py -3.11 -m ruff check .` 通过；`py -3.11 -m pytest -q` 通过（57 passed）；`py -3.11 -m alembic upgrade head` 通过；`py -3.11 -m alembic current` 输出 `20260627_0007 (head)`；填入 COS 配置后，用 `test/img` 下 JPG 通过真实 COS 适配器调用 `/api/v1/files/images`，生成 `thumb_sm`、`thumb_md`、`preview_lg`、`display` 4 个对象，返回 `asset_id=218b5ee2-70c3-4652-b842-7f93205b0569`；本次 COS 写入实测使用临时 SQLite 元数据环境，直接连接当前 `.env` 数据库时曾遇到连接超时，因此未把该测试资产持久化到远程库；对 `thumb_md` URL 做匿名 HEAD 返回 403，说明当前 Bucket 读权限不是公有读。
- 阻塞：前端尚未接入文件上传接口；猫咪库、任务等业务表尚未落地，本切片只完成文件资产通用能力和个人资料头像资产字段；当前 COS Bucket 匿名读取返回 403，如前端要直接展示 `default_url` / `default_thumb_url`，需要将 Bucket 调整为公有读，或后续实现签名 URL 返回策略。
- 下一步：确认 COS Bucket 读策略（公有读或签名 URL）并接个人中心头像更新接口，再按猫咪库、地图点位、任务打卡顺序接业务表。

### 2026-06-25

- 分支：feature/map-navigation
- 完成：修复地图抽屉拖动后高德地图容器未 resize 导致的白屏/比例异常；将 `NS_BINDING_ABORTED` 等请求取消从普通网络错误中拆分，地图页忽略取消请求，避免误触发未登录/错误态；地图页首次进入默认不加载 marker，用户选择筛选层或搜索后再加载；H5 marker 支持 icon、label、preview 小卡片三态；“我的位置”会记录并显示当前位置；导航改为在本页内用高德 Walking 绘制路线，小程序端回落到原生 map polyline，不再 `window.open` 外部高德 URL；搜索合并后端内部点位与高德 PlaceSearch 结果，小程序端提供高德 REST POI 搜索回落，并按湖北师范大学校园 bounds 过滤区域外 POI，外部搜索点位会生成本地详情卡并可直接按坐标导航；筛选菜单改为 SVG 图标列表和 CSS 箭头。
- 修改文件：`frontend/src/services/request.ts`、`frontend/src/api/map.ts`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/tests/services/request.test.ts`、`frontend/tests/api/map.test.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/plans/2026-06-25-map-navigation.md`、`docs/开发进度.md`
- API 或表变更：无后端表结构变更；前端 `getMapPointNavigation` 调用支持 `from_lng`、`from_lat`、`mode=walking` 查询参数，后端现有接口保持兼容。
- 迁移：无。
- 验证：`npm test -- --run tests/services/request.test.ts tests/pages/map-page.test.ts tests/api/map.test.ts tests/components/app-tab-bar.test.ts` 通过（31 tests）；`npm run type-check` 通过；`py -3.11 -m pytest tests\test_map_api.py -q` 在 `backend` 目录通过（6 passed）；`npm run build:h5` 通过；`npm run build:mp-weixin` 通过；开发中曾用 H5 浏览器确认地图页切到其它一级页不会触发 `App Launch` 递增。
- 阻塞：worktree 基线中 `frontend/素材/svg/地图点` 还没有命名清晰的 `猫咪点.svg`、`地标.svg`、`我的位置.svg`，当前猫咪/地标筛选图标临时映射到 `1.svg`、`10.svg`，后续可统一素材命名后替换；小程序端高德 REST POI 搜索需要在微信公众平台配置 `https://restapi.amap.com` 为 request 合法域名，并确认当前高德 Key 具备 Web 服务调用权限。
- 下一步：联调真实高德 Key 和微信开发者工具环境，验证小程序端定位权限、路线 polyline 展示和 PlaceSearch 平台适配。

- 分支：feature/profile-onboarding
- 完成：实现首次登录强制修改密码和初始化身份前后端切片；登录展示改为喵喵号；管理员创建成员时可自动生成 `trmx0001` 形式喵喵号并默认使用喵喵号作为初始密码；改密成功后返回新 access token 并进入身份初始化；初始化身份保存昵称、头像地址、部门和手机号，手机号仅管理员可见；资料未初始化时地图业务接口返回 `63006`；修复初始资料缺失时写入/展示 `????` 的问题，缺失字段保持空值；移除初始化身份页底部“小提示”卡片，并在 `AGENTS.md` 记录手机微信小程序视口优先原则；按“我的”页原型完成个人中心首页、统计入口、收藏猫咪入口、退出登录、个人资料查看与编辑、管理员入口和添加账户页面。
- 修改文件：`AGENTS.md`、`backend/app/modules/{auth,profile,me}/**`、`backend/app/api/v1/{auth,admin_users,map,me,profile,router}.py`、`backend/alembic/versions/20260625_0005_add_profile_completion_fields.py`、`backend/alembic/versions/20260625_0006_clear_profile_question_placeholders.py`、`backend/tests/test_{auth_api,admin_users_api,profile_api,map_api,me_api}.py`、`frontend/src/api/{auth,profile,me,admin-users}.ts`、`frontend/src/stores/user.ts`、`frontend/src/services/app-startup.ts`、`frontend/src/pages/login/index.vue`、`frontend/src/pages/auth/change-password.vue`、`frontend/src/pages/profile/**`、`frontend/src/pages/admin/**`、`frontend/src/pages.json`、`frontend/tests/**`、`docs/plans/2026-06-25-profile-center*.md`、`docs/接口文档/**`、`docs/库表文档/用户与个人中心模块_库表设计文档.md`。
- API 或表变更：新增 `GET /api/v1/profile/me`、`PATCH /api/v1/profile/me`、`POST /api/v1/profile/me/complete`、`GET /api/v1/me/dashboard`、`GET /api/v1/me/tasks`、`GET /api/v1/me/checkins`、`GET /api/v1/me/observations`、`GET /api/v1/me/favorite-cats`；`PATCH /api/v1/auth/password` 成功后返回新 token；登录和管理员创建账号兼容 `meow_no`；`user_profiles` 新增 `profile_completed`、`profile_completed_at`。
- 迁移：新增并已执行 `20260625_0005_add_profile_completion_fields.py`、`20260625_0006_clear_profile_question_placeholders.py`，当前 Alembic 版本为 `20260625_0006 (head)`。
- 验证：先用新增回归测试复现缺陷：`pytest backend\tests\test_admin_users_api.py backend\tests\test_profile_api.py -q` 出现 2 个失败，`npm run test -- tests/pages/profile-complete-page.test.ts tests/styles/global-font.test.ts` 出现 2 个失败；`pytest backend\tests\test_me_api.py -q` 出现 4 个 404 失败；`pytest backend\tests\test_profile_api.py -q` 出现 2 个 405 失败；`npm run test -- tests/api/me.test.ts tests/api/profile.test.ts tests/api/admin-users.test.ts` 出现缺失 API 失败；`npm run test -- tests/pages/profile-page.test.ts tests/pages/admin-page.test.ts` 出现缺失页面失败。修复后目标测试通过。最终 `py -3.11 -m alembic upgrade head` 通过，`py -3.11 -m alembic current` 输出 `20260625_0006 (head)`；数据库复查纯问号 profile 占位数量为 0；`pytest -q` 通过，49 个测试通过；`ruff check .` 通过；`npm run test` 通过，16 个测试文件、61 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：未在微信开发者工具中做真机/模拟器手动验收；头像当前只完成前端大小限制和本地路径占位，正式持久化上传待文件模块接入；任务、猫咪收藏、观察记录等业务表尚未落地，本切片对应子菜单先返回空分页并展示空状态。
- 下一步：用真实账号联调登录 -> 改密 -> 初始化身份 -> 我的页 -> 资料编辑 -> 管理员添加账户全流程；后续任务/猫咪库模块完成后，将个人中心统计和子菜单从空分页切换到真实聚合数据。

### 2026-06-24

- 分支：fix/frontend-tabbar
- 完成：去除了地图页底部的“高德地图” Logo 和版权信息水印，提升了沉浸式地图体验。
- 修改文件：`frontend/src/pages/index/index.vue`
- 验证：执行 `npm run type-check` 类型检查通过。
- 阻塞：无。
- 下一步：继续其他体验优化。

### 2026-06-24

- 分支：fix/frontend-tabbar
- 完成：修复了地图等页面在通过底部导航切换时重复请求数据、页面抖动和重新挂载的问题。在 `pages.json` 中配置了原生的 `tabBar`，使 UniApp 能够将页面状态保存在内存中；修改 `AppTabBar.vue` 使用 `uni.switchTab` 替代原本销毁全部页面的 `uni.reLaunch`；在 `App.vue` 中调用 `uni.hideTabBar()` 隐藏自带底部栏，以继续使用原本设计的精美自定义组件。
- 修改文件：`frontend/src/pages.json`、`frontend/src/components/AppTabBar.vue`、`frontend/src/App.vue`
- 验证：执行 `npm run type-check` 类型检查通过。
- 阻塞：无。
- 下一步：真机或微信开发者工具中验收页面切换是否平滑以及数据是否不再重复请求。


### 2026-06-24

- 分支：feature/map-drawer-drag
- 完成：确认前端地图页出现的“图书馆”等绿色点位不是前端静态数据，而是后端 `20260624_0003_create_map_tables.py` 迁移写入的示例 `map_points`；新增清理迁移删除 5 条固定示例点位，当前开发库已执行迁移并复查这些 ID 剩余数量为 0。
- 修改文件：`backend/alembic/versions/20260624_0004_remove_sample_map_points.py`、`backend/tests/test_map_seed_cleanup_migration.py`、`docs/开发进度.md`
- API 或表变更：无新增 API；新增 Alembic 迁移 `20260624_0004_remove_sample_map_points.py`，只清理旧示例 `map_points` 数据，不改表结构。
- 迁移：已执行 `py -3.11 -m alembic upgrade head`，从 `20260624_0003` 升级到 `20260624_0004`。
- 验证：先运行 `py -3.11 -m pytest tests/test_map_seed_cleanup_migration.py -q` 观察到清理迁移缺失导致失败；补迁移后该测试通过；`py -3.11 -m pytest tests/test_map_api.py -q` 通过；数据库复查示例点位 ID 数量为 0。
- 阻塞：未在微信开发者工具中重新打开页面做真实视觉验收。
- 下一步：刷新前端地图页联调确认假点位消失；后续面向微信小程序平台时，继续将地图抽屉拖拽和高频触摸计算下沉到 WXS，减少 JS 线程通信带来的卡顿。

### 2026-06-24

- 分支：feature/map-drawer-drag
- 完成：将 access token 默认有效期从 7200 秒调整为 604800 秒；新增 `POST /api/v1/auth/renew` access token 续签接口；前端保存 token 过期时间，并在剩余有效期小于 1 天时先续签再调用 `/auth/me` 或地图接口。
- 修改文件：`backend/app/core/config.py`、`backend/app/core/security.py`、`backend/app/api/v1/auth.py`、`backend/app/modules/auth/service.py`、`backend/app/modules/auth/schemas.py`、`backend/tests/test_auth_api.py`、`backend/.env.example`、`backend/README.md`、`frontend/src/api/auth.ts`、`frontend/src/constants/storage.ts`、`frontend/src/stores/user.ts`、`frontend/src/pages/index/index.vue`、`frontend/tests/api/auth.test.ts`、`frontend/tests/stores/user.test.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/接口文档/鉴权模块_接口文档.md`、`docs/接口文档/接口设计规范文档.md`
- API 或表变更：新增 `POST /api/v1/auth/renew`；无新增表，仍使用 JWT + `users.token_version` 控制旧 token 失效。
- 验证：已局部运行后端鉴权测试、前端 auth/store/map-page 测试；最终交付前需运行完整后端和前端验证。
- 阻塞：无；正在运行的后端服务需要重启后才会读取新的默认 token 有效期。
- 下一步：若需要更强安全边界，可后续增加 `auth_time` 绝对登录期限，目前先按 V1 约束不引入 refresh token 表。

### 2026-06-24

- 分支：feature/map-drawer-drag
- 完成：确认并修复 token 过期后前端不能退出到登录页的问题。根因是请求层在 HTTP 401 时先抛 `ApiHttpError`，没有读取统一响应体中的 `code = 40102`，页面侧也没有统一跳转登录页的兜底；新增认证会话处理服务，请求层识别 `40101/40102` 后清本地登录态并 `reLaunch` 到登录页，应用入口注册 Pinia 清理器以同步清除 Store 状态。
- 修改文件：`frontend/src/services/request.ts`、`frontend/src/services/auth-session.ts`、`frontend/src/services/app-startup.ts`、`frontend/src/main.ts`、`frontend/tests/services/request.test.ts`、`docs/开发进度.md`。
- API 或表变更：无。
- 迁移：无。
- 验证：先运行 `npm run test -- tests/services/request.test.ts` 观察到认证过期处理缺失导致测试失败；修复后 `npm run test -- tests/services/request.test.ts` 通过，1 个测试文件、5 个测试通过；`npm run test` 通过，10 个测试文件、36 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：未用真实过期 JWT 在微信开发者工具中做手动联调。
- 下一步：联调后端时用过期 token 或重置密码后旧 token 验证前端会清空登录态并返回登录页。

### 2026-06-24

- 分支：feature/map-drawer-drag
- 完成：阅读 AGENTS、项目说明、库表说明、接口规范、地图功能/接口/库表文档与地图页面原型；检查前端工作区，发现当前 WXS 抽屉接入导致 `vue-tsc` 无法识别模板中的 `drawer` 模块和 `drawer.wxs.js`；补齐 WXS 类型声明；删除前端地图模块遗留的静态任务点模拟数据，并新增回归测试防止 `MAP_SHELL_ITEMS` 与示例任务点 ID 回流。
- 修改文件：`frontend/src/pages/index/map-page.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/src/env.d.ts`、`frontend/src/shime-uni.d.ts`、`docs/开发进度.md`。当前工作区另有本分支既有 WXS 抽屉接入文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/drawer.wxs`。
- API 或表变更：无。
- 迁移：无。
- 验证：先运行 `npm run test -- tests/pages/map-page.test.ts` 观察到新增测试失败，确认能捕获静态任务点模拟数据；修复后 `npm run test -- tests/pages/map-page.test.ts` 通过，1 个测试文件、7 个测试通过；`npm run type-check` 通过；`npm run test` 通过，10 个测试文件、35 个测试通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过；`rg` 确认 `frontend/src` 中不再包含 `MAP_SHELL_ITEMS`、`task-emergency-north-gate`、`task-daily-canteen`。
- 阻塞：未在微信开发者工具中做真机/模拟器手动拖拽验收；真实任务、猫咪和物资业务表仍待后续模块接入。
- 下一步：在微信开发者工具中验收地图抽屉拖拽、搜索和 marker 点击；后续面向微信小程序平台时，可继续将高频触摸/拖拽计算下沉到 WXS，减少 JS 线程通信导致的卡顿。

### 2026-06-24

- 分支：fix/map-scroll
- 完成：修复首页底部动态内容区域无法滚动的问题；将 `drawer-body` 改为 `scroll-view` 组件，并添加 `flex` 相关样式保证在固定高度的抽屉内可正确触摸滑动。
- 修改文件：`frontend/src/pages/index/index.vue`、`docs/开发进度.md`
- 验证：`npm run type-check` 通过。
- 阻塞：无。
- 下一步：继续其他页面开发。

### 2026-06-24

- 分支：codex/feature/frontend-map-api
- 完成：将 `dev` 后端地图接口合入前端联调分支；新增前端地图 API 封装；地图首页从静态 `MAP_SHELL_ITEMS` 切换到 `/api/v1/map/init`、`/map/points`、`/map/search`、`/map/bottom-content`、`/map/points/{point_id}/summary` 和 `/map/points/{point_id}/navigation`；H5 高德地图和小程序原生 map 均接入后端点位 marker；点击 marker 或列表项展示后端点位卡片。
- 修改文件：`frontend/src/api/map.ts`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/tests/api/map.test.ts`、`frontend/tests/pages/map-page.test.ts`、`docs/开发进度.md`，并合入 `dev` 中的后端地图模块文件。
- API 或表变更：前端接入后端地图成员端接口；后端表结构来自已合入的 `20260624_0003_create_map_tables.py`。
- 迁移：`20260624_0003_create_map_tables.py`，当前 Alembic 版本为 `20260624_0003 (head)`。
- 验证：前端 `npm run test` 通过，10 个测试文件、36 个测试通过；`npm run type-check` 通过；`npm run build:h5` 通过；`npm run build:mp-weixin` 通过。后端 `py -3.11 -m pytest tests/test_map_api.py -q` 通过，6 个测试通过；`py -3.11 -m pytest -q` 通过，30 个测试通过；`py -3.11 -m ruff check .` 通过；`py -3.11 -m alembic current` 输出 `20260624_0003 (head)`。
- 阻塞：真实任务、猫咪和物资业务表尚未接入，当前地图卡片业务摘要仍由地图层点位数据支撑。
- 下一步：在微信开发者工具中用真实登录态做地图页面手动验收；后续任务、猫咪库、物资模块完成后，将点位卡片动作跳转到真实详情页。

### 2026-06-24

- 分支：codex/chore/frontend-songti-font
- 完成：将前端全局中文字体统一为宋体字体栈，并在 `AGENTS.md` 记录为前端开发规范；新增回归测试防止全局字体入口或规范被误删。
- 修改文件：`frontend/src/App.vue`、`frontend/tests/styles/global-font.test.ts`、`AGENTS.md`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：`npm run test -- tests/styles/global-font.test.ts` 通过，1 个测试文件、2 个测试通过；`npm run test` 通过，9 个测试文件、30 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：无。
- 下一步：后续前端页面开发继续沿用全局宋体字体栈；如有 SVG 或生成素材包含中文，也按该字体规范处理。

### 2026-06-24

- 分支：codex/feature/backend-map-support
- 完成：检查前端地图首页壳层进度；阅读地图功能、接口与库表文档；从页面原型代码确认高德 Web Key 与 securityJsCode；新增后端地图模块模型、服务和 `/api/v1/map` 第一批成员端接口，覆盖初始化、点位列表、搜索、点位卡片、导航信息和底部动态内容。
- 修改文件：`backend/app/modules/map/**`、`backend/app/api/v1/map.py`、`backend/app/api/v1/router.py`、`backend/app/db/types.py`、`backend/app/core/config.py`、`backend/app/core/errors.py`、`backend/alembic/env.py`、`backend/alembic/versions/20260624_0003_create_map_tables.py`、`backend/tests/test_map_api.py`、`backend/.env.example`、`backend/README.md`、`docs/plans/2026-06-24-backend-map-support.md`、`docs/开发进度.md`
- API 或表变更：新增 `GET /api/v1/map/init`、`GET /api/v1/map/points`、`GET /api/v1/map/search`、`GET /api/v1/map/points/{point_id}/summary`、`GET /api/v1/map/points/{point_id}/navigation`、`GET /api/v1/map/bottom-content`；新增 `campuses`、`campus_areas`、`map_marker_configs`、`map_points`、`map_point_photos`。
- 迁移：`20260624_0003_create_map_tables.py`，包含湖北师范大学校园配置、Marker 配置和支撑当前前端壳层的首批示例点位。
- 验证：开发中已运行 `py -3.11 -m pytest tests/test_map_api.py -q`，地图接口测试通过；最终合并前需运行完整后端测试、ruff 和 Alembic 升级验证。
- 阻塞：真实任务、猫咪和物资业务表尚未接入，当前地图卡片中的业务摘要为地图层占位数据。
- 下一步：前端把静态 `MAP_SHELL_ITEMS` 切换为调用地图接口；后续任务、猫咪库、物资模块完成后再聚合真实业务摘要。

### 2026-06-23

- 分支：codex/feature/frontend-map-shell
- 完成：阅读新增地图接口文档、地图库表文档、地图功能说明、页面原型代码与两个新版首页原型；实现地图首页前端壳层，接入高德 H5 地图绘制、湖北师范大学中心点与扩展边界、动态内容抽屉展开/收缩、搜索框交互、全部标记下拉菜单和新的我的位置按钮；本切片暂不渲染业务 marker。
- 修改文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/map-page.ts`、`frontend/src/config/app-env.ts`、`frontend/src/types/amap.d.ts`、`frontend/tests/pages/map-page.test.ts`、`frontend/.env.example`、`frontend/页面原型/**`、`docs/接口文档/地图模块_接口设计文档.md`、`docs/库表文档/地图模块_库表设计说明.md`、`docs/开发进度.md`
- API 或表变更：无后端实现变更；前端按地图模块接口文档预留地图初始化、点位、搜索与底部动态内容接入点。
- 迁移：无。
- 验证：`npm run test` 通过，8 个测试文件、28 个测试通过；`npm run type-check` 通过；`npm run build:h5` 通过；`npm run build:mp-weixin` 通过；H5 本地浏览器在 402×874 视口验证高德画布渲染、默认展开态、折叠态、搜索交互、无纵向滚动、旧搜索/扫一扫/图层入口已移除。
- 阻塞：后端地图 Key、地图初始化/点位/搜索/动态内容数据尚未接入；真实业务 marker 按本次范围暂不实现。
- 下一步：后端补齐本次地图任务需要的 Key、点位与搜索数据后，进行地图接口联调并接入真实 marker。

### 2026-06-23

- 分支：main
- 完成：将 `fix/remove-tab-page-titles` 合并到 `main`，包含底部导航组件、四个一级页面接入、缺省页组件、tab 页隐藏原生导航栏修复，以及默认 SVG 素材整理。
- 修改文件：`frontend/src/components/**`、`frontend/src/pages/**`、`frontend/src/pages.json`、`frontend/src/App.vue`、`frontend/tests/components/**`、`frontend/素材/svg/默认/**`、`docs/plans/**`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：合并后在 `main` 执行 `npm run test` 通过，7 个测试文件、23 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：无；四个一级页面当前仍为业务占位页。
- 下一步：从 `main` 开始地图模块，先实现地图首页业务骨架、点位 API 契约和地图资源初始化接入。

### 2026-06-23

- 分支：codex/feature/frontend-tab-bar
- 完成：阅读缺省页原型和 `frontend/素材/svg/缺省页` 素材；新增可复用 `DefaultState` 缺省页组件和 `default-state` 状态配置；覆盖功能建设中、数据为空、记录为空、搜索为空、网络异常、权限不足、404、位置为空、图片失败、消息为空等常用状态；将地图、猫咪库、任务、我的四个当前占位页替换为缺省页，并复用加载页背景。
- 修改文件：`frontend/src/components/DefaultState.vue`、`frontend/src/components/default-state.ts`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/cats/index.vue`、`frontend/src/pages/tasks/index.vue`、`frontend/src/pages/profile/index.vue`、`frontend/tests/components/default-state.test.ts`、`docs/plans/2026-06-23-frontend-default-states.md`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：`npm run test` 通过，7 个测试文件、23 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过；H5 本地浏览器在 402×874 视口验证四个一级页缺省态渲染、背景/插画资源加载和无纵向滚动。
- 阻塞：无；图片失败、消息为空、权限不足、404 等状态已提供配置，具体页面路由后续按模块接入。
- 下一步：review 后合并到底部导航相关分支或 `main`；后续进入地图首页业务骨架。

### 2026-06-23

- 分支：codex/feature/frontend-tab-bar
- 完成：阅读首页、猫咪库、任务、我的页面原型；新增公共底部导航组件，使用 `frontend/素材/svg/默认` 四个 SVG；新增猫咪库、任务、我的占位页并接入四个一级页面；底部菜单加入悬浮圆角、激活态上浮缩放和切换动画；按 review 反馈移除激活小点、地图激活图标保留原 SVG 色彩，并将四个一级页适配为移动端无整页上下滑动。
- 修改文件：`frontend/src/App.vue`、`frontend/src/components/AppTabBar.vue`、`frontend/src/components/app-tab-bar.ts`、`frontend/src/pages.json`、`frontend/src/pages/index/index.vue`、`frontend/src/pages/cats/index.vue`、`frontend/src/pages/tasks/index.vue`、`frontend/src/pages/profile/index.vue`、`frontend/tests/components/app-tab-bar.test.ts`、`docs/plans/2026-06-23-frontend-tab-bar.md`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：`npm run test` 通过，6 个测试文件、19 个测试通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过；H5 本地浏览器在 402×874 视口验证四个一级页无纵向滚动、底部无激活小点、地图激活图标保留原色。
- 阻塞：无；四个一级页面目前仅为导航占位，不包含业务数据。
- 下一步：review 后合并到 `main`；后续按模块开发地图首页、猫咪库、任务和个人中心业务内容。

### 2026-06-23

- 分支：codex/fix/frontend-favicon
- 完成：将 `frontend/favicon` 配置为前端静态公共目录，并在 H5 入口挂载 `favicon.ico`、16/32 PNG、Apple touch icon 与 `site.webmanifest`。
- 修改文件：`frontend/index.html`、`frontend/vite.config.ts`、`frontend/favicon/**`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无。
- 验证：`npm run build:h5` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：无。
- 下一步：合并到 `main` 后继续推进前后端鉴权联调。

### 2026-06-23

- 分支：main
- 完成：将 `feature/frontend-loading` 合并到 `main`，包含前端工程初始化、前端基础层、登录页与鉴权前端、加载页与启动流程、素材和页面原型整理。
- 修改文件：`frontend/**`、`frontend/素材/**`、`frontend/页面原型/**`、`frontend/页面原型代码/**`、`AGENTS.md`、`docs/开发进度.md`
- API 或表变更：无新增后端 API 或表；前端已按 `/api/v1/auth/captcha`、`/api/v1/auth/login`、`/api/v1/auth/me`、`/api/v1/auth/password`、`/api/v1/auth/logout` 契约封装。
- 验证：合并前在 `feature/frontend-loading` 执行 `npm run type-check`、`npm run test`、`npm run build:mp-weixin` 通过；合并后在 `main` 重复执行以上前端验证。
- 阻塞：真实地图点位、任务数据、用户角色扩展资源接口尚未开发；加载页当前通过 `initializeAppResources` 预留可控入口。
- 下一步：启动后端服务后进行前端登录页真实联调；随后推进地图首页骨架和地图数据接口。

### 2026-06-23

- 分支：fix/dev-cors
- 完成：修复本地开发环境 H5 调用后端时的 CORS 限制；后端现在通过 `CATMAP_CORS_ALLOW_ORIGIN_REGEX` 放开 `localhost`、`127.0.0.1`、`[::1]` 的任意端口，并回显具体 Origin；补充 README 与 `.env.example` 说明，标记部署时应清空开发正则、收紧来源，并优先使用 nginx 同源代理。
- 修改文件：`backend/app/core/config.py`、`backend/app/main.py`、`backend/.env.example`、`backend/README.md`、`backend/tests/test_cors.py`、`docs/开发进度.md`
- API 或表变更：无。
- 迁移：无新增；当前数据库版本为 `20260623_0002 (head)`。
- 验证：先运行 `py -3.11 -m pytest tests/test_cors.py -q` 复现并修复 CORS 回归测试；修复后 `py -3.11 -m pytest -q` 通过，24 个测试通过；`py -3.11 -m ruff check .` 通过；`py -3.11 -m alembic current` 输出 `20260623_0002 (head)`。
- 阻塞：无；部署环境需要重新收紧 CORS，建议前端与后端通过 nginx 走同源代理。
- 下一步：前端本机测试时重启后端服务使新 CORS 配置生效；部署前清空开发 CORS 正则，并通过 nginx 同源代理收紧访问。

- 分支：main
- 完成：将后端集成分支 `dev` 合并到 `main`，包含后端骨架、数据库基础设施与鉴权模块。
- 修改文件：`backend/**`、`docs/开发进度.md`
- API 或表变更：`main` 已包含 `GET /api/v1/health`、鉴权接口、管理员成员管理基础接口，以及 `users`、`auth_captchas`、`user_profiles`、`admin_operation_logs` 表。
- 迁移：`main` 当前迁移版本为 `20260623_0002 (head)`。
- 验证：合并前在 `dev` 执行 `py -3.11 -m pytest -q` 通过，23 个测试通过；`py -3.11 -m ruff check .` 通过；`py -3.11 -m alembic current` 输出 `20260623_0002 (head)`。合并后在 `main` 重复执行以上三项验证，结果全部通过。
- 阻塞：无；真实前端页面联调仍需在前端合并后执行。
- 下一步：以前后端均在 `main` 的状态进行鉴权联调。

### 2026-06-23

- 分支：feature/auth
- 完成：实现鉴权模块后端核心，包括用户、验证码、资料、管理员操作日志模型与迁移；实现验证码、登录、当前用户、修改密码、退出登录，以及管理员成员管理基础接口。
- 修改文件：`backend/pyproject.toml`、`backend/.env.example`、`backend/README.md`、`backend/app/api/v1/**`、`backend/app/core/**`、`backend/app/modules/auth/**`、`backend/alembic/versions/20260623_0002_create_auth_tables.py`、`backend/tests/**`、`docs/plans/2026-06-23-auth-module.md`、`docs/开发进度.md`
- API 或表变更：新增鉴权与管理员成员管理接口；新增 `users`、`auth_captchas`、`user_profiles`、`admin_operation_logs` 表。
- 迁移：`20260623_0002_create_auth_tables.py`。
- 验证：`py -3.11 -m pytest -q` 通过，23 个测试通过；`py -3.11 -m ruff check .` 通过；`py -3.11 -m alembic upgrade head` 应用到 `20260623_0002`；HTTP 契约联调覆盖 captcha、login、me、logout、change-password。
- 阻塞：批量导入成员接口本切片未做，后续管理员页面需要时单独实现。
- 下一步：与前端登录页做真实联调。

### 2026-06-23

- 分支：feature/backend-foundation
- 完成：搭建 FastAPI 后端骨架、统一成功/错误响应、trace_id 中间件、健康检查、SQLAlchemy 2.0 engine/session、Alembic 配置、PostGIS/pgcrypto 初始迁移和 ruff 配置。
- 修改文件：`backend/pyproject.toml`、`backend/README.md`、`backend/app/**`、`backend/alembic/**`、`backend/tests/**`、`docs/plans/**`、`docs/开发进度.md`
- API 或表变更：新增 `GET /api/v1/health`；新增统一错误响应处理。
- 迁移：`20260623_0001_enable_postgis_pgcrypto.py`。
- 验证：`py -3.11 -m pytest -q` 通过；`py -3.11 -m ruff check .` 通过；`py -3.11 -m alembic upgrade head` 通过；SQLAlchemy 查询确认 `pgcrypto` 与 `postgis` 存在。
- 阻塞：无。
- 下一步：开始鉴权模块。

### 2026-06-22

- 分支：feature/frontend-loading
- 完成：阅读加载页原型和鉴权接口文档；新增加载页作为小程序启动页，使用加载页背景、会徽、猫爪和麦穗素材；将三点加载改为猫爪渐变动画；新增三句加载文案循环渐变/淡出；新增启动流程服务。
- 修改文件：`frontend/src/pages/loading/index.vue`、`frontend/src/pages.json`、`frontend/src/services/app-startup.ts`、`frontend/tests/services/app-startup.test.ts`、`frontend/素材/加载页素材`
- 验证：`npm run test` 通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：真实地图点位、任务数据、用户角色扩展资源接口尚未开发。
- 下一步：将地图点位、任务和角色资源接入加载页初始化流程。

### 2026-06-21

- 分支：feature/frontend-login
- 完成：阅读登录页原型和鉴权接口文档；新增 `api/auth`，覆盖验证码、登录、当前用户、修改密码、退出登录接口；扩展 `useUserStore` 登录、刷新用户、退出登录动作；新增登录页、用户协议弹窗和鉴权状态流转。
- 修改文件：`frontend/src/api/auth.ts`、`frontend/src/stores/user.ts`、`frontend/src/pages/login/index.vue`、`frontend/src/pages.json`、`frontend/tests/**`、`frontend/素材/**`、`docs/开发进度.md`
- 验证：`npm run test` 通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：首次改密页尚未实现。
- 下一步：与后端鉴权接口联调登录链路。

### 2026-06-21

- 分支：feature/frontend-foundation
- 完成：建立 `frontend/src` 目录规范，新增环境配置、统一请求封装、API/用户类型、存储 key 常量、Pinia 用户状态骨架和 Vitest 单元测试。
- 修改文件：`frontend/package.json`、`frontend/package-lock.json`、`frontend/vitest.config.ts`、`frontend/src/**`、`frontend/tests/**`、`frontend/.env.example`、`docs/开发进度.md`
- 验证：`npm run test` 通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：无。
- 下一步：开发登录页和鉴权 API 模块。

### 2026-06-21

- 分支：feature/frontend-init
- 完成：创建 uni-app Vue3 TypeScript 前端空项目，设置微信小程序默认开发/构建脚本，接入 Pinia；更新 `AGENTS.md` 前端流程；将素材、页面原型、页面原型代码迁移到 `frontend` 目录。
- 修改文件：`AGENTS.md`、`frontend/**`、`docs/开发进度.md`
- 验证：`npm run type-check` 通过；`npm run build:mp-weixin` 通过。
- 阻塞：无。
- 下一步：建立前端基础目录规范、请求层和登录页。
