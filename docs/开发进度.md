# 开发进度

## 当前总览

- 当前阶段：MVP
- 当前重点模块：加载页与启动流程
- 当前分支：feature/frontend-loading
- 最近更新时间：2026-06-22

## 模块状态

| 模块 | 状态 | 分支 | 负责人/Agent | 备注 |
|---|---|---|---|---|
| 前端工程初始化 | 已完成 | feature/frontend-init | Codex | uni-app Vue3 TypeScript 微信小程序空项目已初始化 |
| 前端素材与原型整理 | 已完成 | feature/frontend-init | Codex | 素材、原型、原型代码已迁移到 frontend |
| 前端基础工程层 | 已完成 | feature/frontend-foundation | Codex | 目录规范、环境配置、请求封装、Pinia 用户状态骨架 |
| 登录页与鉴权前端 | 已完成 | feature/frontend-login | Codex | 登录页、验证码、登录 API、登录状态流转 |
| 加载页与启动流程 | 待联调 | feature/frontend-loading | Codex | 加载页 UI、猫爪动画、加载文案轮播、登录态检查与资源初始化入口 |
| 鉴权模块 | 未开始 |  |  |  |
| 地图模块 | 未开始 |  |  |  |
| 任务模块 | 未开始 |  |  |  |
| 猫咪库模块 | 未开始 |  |  |  |
| 个人中心模块 | 未开始 |  |  |  |
| 管理员模块 | 未开始 |  |  |  |
| 通知模块 | 未开始 |  |  |  |
| 排班与任务指派 | 未开始 |  |  |  |

## 最近进展

### 2026-06-22

- 分支：feature/frontend-loading
- 完成：阅读加载页原型和鉴权接口文档；新增加载页作为小程序启动页，使用加载页背景、会徽和猫爪素材；将三点加载改为猫爪渐变动画；新增三句加载文案循环渐变/淡出；新增启动流程服务，打开小程序后检查登录状态，未登录进入登录页，已登录后执行地图/任务/角色资源初始化入口再进入首页。
- 修改文件：frontend/src/pages/loading/index.vue、frontend/src/pages.json、frontend/src/services/app-startup.ts、frontend/tests/services/app-startup.test.ts、frontend/素材/加载页素材。
- 验证：`npm run test -- tests/services/app-startup.test.ts` 通过；`npm run test` 通过；`npm run type-check` 通过；`npm run build:mp-weixin` 通过；`npm run build:h5` 通过。
- 阻塞：真实地图点位、任务数据、用户角色扩展资源接口尚未开发，当前加载页通过 `initializeAppResources` 预留可控入口。
- 下一步：开发地图首页数据接口或前端地图首页骨架后，将点位、任务和角色资源接入加载页初始化流程。

### 2026-06-21

- 分支：feature/frontend-init
- 完成：创建 uni-app Vue3 TypeScript 前端空项目，设置微信小程序默认开发/构建脚本，接入 Pinia。
- 修改文件：frontend 目录、docs/开发进度.md。
- 验证：`npm run type-check` 通过；`npm run build` 通过，输出 `dist/build/mp-weixin`。
- 阻塞：无。
- 下一步：开始前端基础目录规范、请求层、路由页面骨架或登录页开发。

### 2026-06-21

- 分支：feature/frontend-init
- 完成：更新 `AGENTS.md` 前端开发流程，要求开发页面前查看 `frontend/页面原型` 对应图片并尽量 1:1 还原；约束前端素材只能优先使用 `frontend/素材`，缺失时使用明确占位符。将原根目录 `icon`、`svg` 移入 `素材`，并将 `素材`、`页面原型`、`页面原型代码` 迁移到 `frontend` 目录。
- 修改文件：AGENTS.md、docs/开发进度.md、frontend/素材、frontend/页面原型、frontend/页面原型代码。
- 验证：`npm run type-check` 通过；`npm run build` 通过，输出 `dist/build/mp-weixin`。
- 阻塞：无。
- 下一步：开始前端基础目录规范、请求层、路由页面骨架或登录页开发。

### 2026-06-21

- 分支：feature/frontend-foundation
- 完成：建立 `frontend/src` 目录规范，新增环境配置 `config/app-env`，新增统一请求封装 `services/request`，新增 API/用户类型，新增存储 key 常量，新增 Pinia 用户状态骨架，补充 Vitest 单元测试。
- 修改文件：frontend/package.json、frontend/package-lock.json、frontend/vitest.config.ts、frontend/src、frontend/tests、frontend/.env.example、docs/开发进度.md。
- 验证：`npm run test` 通过；`npm run type-check` 通过；`npm run build` 通过，输出 `dist/build/mp-weixin`。
- 阻塞：无。
- 下一步：基于 `frontend/页面原型/登录页.png` 开发登录页和鉴权 API 模块。

### 2026-06-21

- 分支：feature/frontend-login
- 完成：阅读 `frontend/页面原型/登录页.png` 和鉴权接口文档；新增 `api/auth`，覆盖验证码、登录、当前用户、修改密码、退出登录接口；扩展 `useUserStore` 登录、刷新用户、退出登录动作；新增登录页并作为启动页；使用 `frontend/素材/登录页背景.png` 和 `frontend/素材/登陆页猫猫头.png`，缺失的协会徽标和输入图标以明确占位展示。
- 修改文件：frontend/src/api/auth.ts、frontend/src/stores/user.ts、frontend/src/pages/login/index.vue、frontend/src/pages.json、frontend/tests、frontend/素材、docs/开发进度.md。
- 验证：`npm run test` 通过；`npm run type-check` 通过；`npm run build` 通过；`npm run build:h5` 通过；H5 静态预览确认背景和猫图加载正常。
- 阻塞：协会徽标、学号/密码/验证码/眼睛/刷新等独立图标素材缺失，当前按规则使用占位。
- 下一步：补真实图标素材后替换占位，或继续开发首次改密页/首页骨架。
