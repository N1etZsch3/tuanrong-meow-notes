export const STORAGE_KEYS = {
  accessToken: "cat_map_access_token",
  accessTokenExpiresAt: "cat_map_access_token_expires_at",
  currentUser: "cat_map_current_user",
  agreementAcceptedAccounts: "cat_map_agreement_accepted_accounts",
} as const;
